---
phase: 10-st-006-automation-debt
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - Claude skills build for Cubelets/files/automation-debt-detector.skill
autonomous: false
requirements: [AB-08]

must_haves:
  truths:
    - "Claude skill guides a multi-step detection workflow for automation debt"
    - "Skill references detect_automation_debt MCP tool"
    - "Skill is a valid ZIP with SKILL.md + references/ directory"
  artifacts:
    - path: "Claude skills build for Cubelets/files/automation-debt-detector.skill"
      provides: "Claude skill ZIP for automation debt detection workflow"
  key_links:
    - from: "automation-debt-detector.skill"
      to: "detect_automation_debt"
      via: "MCP tool reference in SKILL.md"
      pattern: "detect_automation_debt"
---

<objective>
Verify the existing automation-debt-detector Claude skill ZIP is valid and perform sandbox verification of the complete ST-006 three-layer stack.

Purpose: The skill file already exists as a binary ZIP in the worktree. This plan validates its structure and runs the final human verification checkpoint for the artifact in Claude.ai sandbox.
Output: Verified skill + sandbox-confirmed artifact
</objective>

<execution_context>
@C:/Users/ricco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ricco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-st-006-automation-debt/10-CONTEXT.md
@.planning/phases/10-st-006-automation-debt/10-01-SUMMARY.md
@.planning/phases/10-st-006-automation-debt/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify automation-debt-detector skill ZIP structure and content</name>
  <files>Claude skills build for Cubelets/files/automation-debt-detector.skill</files>
  <action>
    The skill file already exists as a binary ZIP archive. Verify its structure matches the established pattern (SKILL.md + references/ directory) and that it references the detect_automation_debt MCP tool.

    Run the automated verification script. If the ZIP is invalid or missing required content, recreate it using Python's zipfile module following the agent-feedback-analyzer.skill and tool-stack-analyzer.skill patterns.

    **Expected ZIP structure:**
    ```
    automation-debt-detector/
      SKILL.md           (references detect_automation_debt, 5-step workflow)
      references/
        automation-debt-reference.md  (quick reference card)
    ```

    **SKILL.md must contain:**
    - "detect_automation_debt" MCP tool reference
    - Multi-step workflow (at least 4 steps)
    - At least 2 worked examples
    - Troubleshooting section

    **If the ZIP is valid, no action needed.** If invalid or corrupted, rebuild the ZIP with:
    - SKILL.md (~3,500 chars): When to Use (trigger phrases for automation debt detection), 5-step Workflow (Describe System, Inventory Layers, Identify Fundamental Solution, Run Detection, Interpret Results), MCP Tool Integration (detect_automation_debt input/output), 3 Examples (LLM validation stack Critical, Agent error handling At Risk, Tool latency Healthy), Troubleshooting
    - references/automation-debt-reference.md (~1,000 chars): 3 layer types, 3 erosion channels, debt score tiers, B1/B2/R1 reminder
  </action>
  <verify>
    <automated>python -c "
import zipfile, os
path = 'Claude skills build for Cubelets/files/automation-debt-detector.skill'
if not os.path.exists(path):
    print('FAIL: File not found'); exit(1)
size = os.path.getsize(path)
if size < 500:
    print(f'FAIL: File too small ({size} bytes, expected >500)'); exit(1)
try:
    with zipfile.ZipFile(path, 'r') as z:
        names = z.namelist()
        has_skill = any('SKILL.md' in n for n in names)
        has_ref = any('references/' in n for n in names)
        if has_skill:
            skill_content = [z.read(n).decode('utf-8') for n in names if 'SKILL.md' in n][0]
            has_mcp = 'detect_automation_debt' in skill_content
            has_workflow = 'Workflow' in skill_content or 'workflow' in skill_content
            print(f'SKILL.md chars: {len(skill_content)}')
        else:
            has_mcp = has_workflow = False
        print(f'Files: {names}')
        print(f'SKILL.md: {has_skill}')
        print(f'references/: {has_ref}')
        print(f'MCP tool ref: {has_mcp}')
        print(f'Workflow: {has_workflow}')
        print(f'File size: {size} bytes')
        print('OK' if has_skill and has_ref and has_mcp else 'FAIL')
except zipfile.BadZipFile:
    print('FAIL: Not a valid ZIP file'); exit(1)
" 2>&1</automated>
  </verify>
  <done>Skill ZIP is valid, contains SKILL.md referencing detect_automation_debt MCP tool with multi-step workflow, and includes references/ directory. File size > 500 bytes (binary ZIP, not plaintext).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Sandbox verification and UAT for complete ST-006 stack</name>
  <files>Interactive Artifact for Cubelets/automation-debt-simulator.jsx</files>
  <action>
    Present the complete ST-006 three-layer stack for human verification. All automated work is complete from Plans 01, 02, and 03 Task 1.

    What was built and verified:
    - 12-round automation debt simulator artifact (automation-debt-simulator.jsx, 516 lines, violet accent)
    - detect_automation_debt MCP tool (cubelets_mcp_server.py, with test suite)
    - automation-debt-detector Claude skill (.skill ZIP)
    - ST-006 cubelet markdown (6 faces with archetype boundaries, 253 lines)

    Human verification steps:

    1. Artifact sandbox test: Open Claude.ai, start new conversation, drag-and-drop Interactive Artifact for Cubelets/automation-debt-simulator.jsx into chat. Verify:
       - Scenario selection shows 3 cards (LLM Confidence, Agent Edge Cases, Tool Latency) with difficulty labels
       - Select Tool Latency (easiest). Play through 12 rounds making mix of choices
       - 2x2 dashboard updates each round with sparklines and trend arrows
       - Choice cards show trade-off previews (e.g., "Severity -22, Dependency +12")
       - After round 12: grade displayed (A-F), debt gauge (green-to-red arc), B1/B2/R1 diagram, erosion channels
       - Switch to "Boundaries" tab: 3 counter-patterns shown, take 3-question quiz
       - Click "Try Again", play again. After 2nd completion, optimal path comparison appears
       - Accent color is violet/purple (#7c5cfc), NOT orange or teal

    2. Quick file checks:
       - Cubelets/CubeletsMarkdown/ST-006-automation-debt.md has 6 faces with archetype boundaries in Face 6
       - Claude skills build for Cubelets/files/automation-debt-detector.skill opens as ZIP

    3. Test results: All automated tests should already be green from Plans 01 and 02.
  </action>
  <verify>User confirms artifact renders correctly in Claude.ai sandbox and all deliverables are functional. Type "approved" or describe issues.</verify>
  <done>All ST-006 deliverables pass sandbox and functional verification. User approves the complete three-layer stack.</done>
</task>

</tasks>

<verification>
- Skill ZIP valid with SKILL.md + references/
- SKILL.md references detect_automation_debt MCP tool
- Artifact renders in Claude.ai sandbox without errors
- All 3 scenarios playable through 12 rounds
- Post-game diagnosis and boundaries tab functional
- All automated tests from Plans 01 and 02 pass
</verification>

<success_criteria>
- Skill ZIP structure matches established pattern
- Artifact passes Claude.ai sandbox rendering test
- Complete three-layer stack verified: artifact + MCP tool + skill + cubelet
</success_criteria>

<output>
After completion, create `.planning/phases/10-st-006-automation-debt/10-03-SUMMARY.md`
</output>
