---
phase: 10-st-006-automation-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Interactive Artifact for Cubelets/automation-debt-simulator.jsx
autonomous: true
requirements: [AB-02, AB-03, AB-04, AB-05, AB-10]

must_haves:
  truths:
    - "Student can select from 3 AI automation scenarios and begin a 12-round decision game"
    - "Each round presents 4 choices (quick fix, fundamental investment, transition strategy, do nothing) as cards with trade-off previews"
    - "2x2 dashboard grid tracks severity, capacity, dependency, and willingness with sparklines during gameplay"
    - "Post-game diagnosis shows A-F grade, B1/B2/R1 archetype diagram, erosion timeline, and debt gauge"
    - "Named erosion channels (Knowledge Drain, Complexity Creep, Normalization) appear in post-game diagnosis"
    - "Try Again button replays same scenario; optimal path shown only after 2nd attempt"
    - "Archetype boundaries tab shows 3 counter-patterns with 3-question mini-quiz"
  artifacts:
    - path: "Interactive Artifact for Cubelets/automation-debt-simulator.jsx"
      provides: "Complete 12-round scenario-based decision game with post-game diagnosis and archetype boundaries"
      min_lines: 500
  key_links:
    - from: "automation-debt-simulator.jsx"
      to: "SimEngine/stepSim"
      via: "forked simulation engine from ST-003"
      pattern: "SimEngine|stepSim|symptomSeverity|fundamentalCapacity"
    - from: "automation-debt-simulator.jsx"
      to: "post-game diagnosis"
      via: "tab navigation after round 12"
      pattern: "getGrade|DebtGauge|LoopDiagram|erosionChannel"
    - from: "automation-debt-simulator.jsx"
      to: "archetype boundaries"
      via: "boundaries tab with counter-patterns and quiz"
      pattern: "COUNTER_PATTERNS|boundariesQuiz|Novel Problem|Legitimate Triage"
---

<objective>
Build the complete interactive artifact for ST-006: automation-debt-simulator.jsx. This is a 12-round scenario-based decision game where students choose between quick fixes and fundamental solutions in AI automation contexts, then receive post-game diagnosis with B1/B2/R1 archetype mapping, grading, debt gauge, and an archetype boundaries tab.

Purpose: Create the primary learning experience for ST-006 -- students discover automation debt through gameplay, then understand it through post-game analysis and boundary conditions.
Output: Self-contained React JSX artifact (target 700-850 lines)
</objective>

<execution_context>
@C:/Users/ricco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ricco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-st-006-automation-debt/10-CONTEXT.md
@.planning/phases/10-st-006-automation-debt/10-RESEARCH.md

<interfaces>
<!-- Fork from ST-003's burden-shift-simulator.jsx. Key reusable structures: -->

From Interactive Artifact for Cubelets/burden-shift-simulator.jsx:
```javascript
// Color palette -- reuse base, replace accent
const C = {
  bg: "#0d0f16", canvas: "#121420", panel: "#161928", panelBorder: "#1f2440",
  node: "#1a1e32", nodeBorder: "#283050", text: "#e2e6f0", textSec: "#7580a0",
  textMuted: "#404a68", accent: "#ff8c42", // ST-003 orange -- REPLACE for ST-006
  blue: "#6b8aff", pink: "#ff6b8a", purple: "#a78bfa", danger: "#ff5555", gold: "#ffd700",
};

// Simulation engine -- REUSE with 4-metric composite grading
function SimEngine(scenario) {
  return { symptomSeverity: 70, fundamentalCapacity: 100, orgWill: 100,
           fixDependency: 0, cumulativeCost: 0, round: 0, history: [], choices: [] };
}

// stepSim -- REUSE core formula structure
function stepSim(state, choice, scenario) { /* switch on choice, clamp metrics */ }

// MiniChart -- REUSE VERBATIM for sparklines
function MiniChart({ data, dataKey, color, label, height = 80, maxVal = 100 }) { /* SVG polyline sparkline */ }

// Scenario data structure template
const SCENARIOS = {
  key: {
    title, subtitle, symptom,
    symptomatic: { label, desc, quickEffect, erosionRate, erosionChannel },
    fundamental: { label, desc, delay, buildRate, longEffect },
    transition: { label, target },
    erosionChannels: ["Knowledge Drain", "Complexity Creep"], // NEW for ST-006
  },
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build scenario selection and 12-round game engine</name>
  <files>Interactive Artifact for Cubelets/automation-debt-simulator.jsx</files>
  <action>
    Fork burden-shift-simulator.jsx and create automation-debt-simulator.jsx. This task builds the scenario selection screen, game engine, round UI with 2x2 dashboard, and choice cards. Target ~400-450 lines for this portion (post-game added in Task 2).

    **Accent color:** Use electric violet #7c5cfc (per research recommendation). Replace all ST-003 orange references. Add to C object: `accent: "#7c5cfc", accentGlow: "#7c5cfc44"`.

    **3 scenarios (from RESEARCH.md with exact parameters):**

    1. `llm_confidence` -- "LLM Output Reliability Pipeline": quickEffect 28, erosionRate 10, delay 4, buildRate 14, longEffect 50, erosionChannels: ["Knowledge Drain", "Complexity Creep"]
    2. `agent_edge_cases` -- "AI Agent Error Handling": quickEffect 25, erosionRate 12, delay 5, buildRate 12, longEffect 55, erosionChannels: ["Complexity Creep", "Normalization"]
    3. `tool_latency` -- "MCP Tool Call Performance": quickEffect 22, erosionRate 8, delay 3, buildRate 16, longEffect 45, erosionChannels: ["Knowledge Drain", "Normalization"]

    Each scenario object must include: title, subtitle, symptom, symptomatic (label, desc, quickEffect, erosionRate, erosionChannel), fundamental (label, desc, delay, buildRate, longEffect), transition (label, target), erosionChannels array. Use the full scenario details from 10-RESEARCH.md.

    **Simulation engine:** Reuse SimEngine() and stepSim() from ST-003 with these changes:
    - Add `attemptNumber: 1` to SimEngine initial state (track replays for optimal path)
    - Keep the `do nothing` choice from ST-003 (severity grows, orgWill +2, cost +3)
    - stepSim returns new object (no mutation), history array tracks all 4 metrics per round

    **Grading logic:** Replace ST-003's choice-count grading with 4-metric composite (from RESEARCH.md):
    ```
    severityScore = (100 - symptomSeverity) / 100  // low severity = good
    capacityScore = fundamentalCapacity / 100      // high capacity = good
    dependencyScore = (100 - fixDependency) / 100  // low dependency = good
    willingnessScore = orgWill / 100               // high willingness = good
    composite = severity*0.3 + capacity*0.3 + dependency*0.2 + willingness*0.2
    Grades: >=0.80 A, >=0.70 B+, >=0.60 B, >=0.45 C, >=0.30 D, else F
    ```
    Each grade needs: grade letter, color, 1-line message. Use teal for A/B+, blue for B, gold for C, orange for D, danger red for F.

    **Pre-computed optimal paths:** Store a static `OPTIMAL_PATHS` object per scenario with the expert sequence (e.g., 2 rounds symptomatic, 1 transition, 9 fundamental) and resulting final metrics. These are compared against student results on 2nd attempt.

    **Scenario selection screen:** Card-based grid (3 cards). Each card shows: title, subtitle, difficulty tag (Easy/Medium/Hard based on delay -- tool_latency=Easy, llm_confidence=Medium, agent_edge_cases=Hard). Cards have hover effect with accent border glow. Click starts game.

    **Round UI layout:**
    - Top bar: scenario title, round counter ("Round 3 of 12"), accent-colored progress bar
    - Left: symptom description panel + 4 choice cards in vertical stack
    - Right: 2x2 dashboard grid with MiniChart sparklines (reuse from ST-003 verbatim, adjust width for 2x2 fit)

    **Choice cards:** Each card shows: action name (bold), 1-line description, immediate effect preview (e.g., "Severity -28, Dependency +12") in muted text. 4th card is "Do Nothing" styled differently (dimmer, no effect preview, italic "Inaction has consequences"). Cards have hover highlight with accent border. Clicking a card calls stepSim and advances round.

    **2x2 dashboard grid:** 4 metric panels arranged in 2x2 CSS grid. Each panel: metric name, current value (large number), trend arrow (up/down/flat comparing to 2 rounds ago), MiniChart sparkline. Metric colors: Severity = danger red, Capacity = teal, Dependency = accent violet, Willingness = blue.

    **State management:** Use array-based state only (no Set). Key states:
    - phase: 'select' | 'playing' | 'results'
    - scenario: string key
    - sim: simulation state object
    - attemptNumber: number (1 or 2+)

    **Sandbox constraints:** Inline styles only, no external imports beyond React. Explicit z-index on any overlapping elements. Keep scenario descriptions terse to stay under token limit.
  </action>
  <verify>
    <automated>python -c "
content = open('Interactive Artifact for Cubelets/automation-debt-simulator.jsx', encoding='utf-8').read()
checks = {
    'SimEngine': 'SimEngine' in content,
    'stepSim': 'stepSim' in content,
    'MiniChart': 'MiniChart' in content,
    'llm_confidence': 'llm_confidence' in content,
    'agent_edge_cases': 'agent_edge_cases' in content,
    'tool_latency': 'tool_latency' in content,
    'accent_violet': '#7c5cfc' in content,
    'getGrade': 'getGrade' in content or 'composite' in content,
    'attemptNumber': 'attemptNumber' in content,
    'OPTIMAL_PATHS': 'OPTIMAL_PATHS' in content or 'optimalPath' in content.lower(),
}
passed = sum(v for v in checks.values())
for k, v in checks.items():
    print(f'  {k}: {\"PASS\" if v else \"FAIL\"}')
lines = len(content.split('\n'))
print(f'Lines: {lines}')
print(f'Result: {passed}/{len(checks)} checks passed')
print('OK' if passed >= 8 else 'FAIL')
" 2>&1</automated>
  </verify>
  <done>automation-debt-simulator.jsx exists with 3 AI scenarios, SimEngine/stepSim game engine, 4-metric composite grading, 2x2 dashboard with MiniChart sparklines, choice cards with trade-off previews, and electric violet accent color. Scenario selection, round play, and game completion flow all functional.</done>
</task>

<task type="auto">
  <name>Task 2: Add post-game diagnosis, archetype boundaries tab, and replay mechanics</name>
  <files>Interactive Artifact for Cubelets/automation-debt-simulator.jsx</files>
  <action>
    Extend the artifact built in Task 1 with the post-game results screen, archetype boundaries tab, and Try Again mechanics. Target total file size: 700-850 lines.

    **Post-game results screen (phase='results'):**
    Tab navigation with two tabs: "Diagnosis" (default) and "Boundaries". Use array-state tab pattern: `const [activeTab, setActiveTab] = useState('diagnosis')`.

    **Diagnosis tab contents (4 sections in scrollable column):**

    1. **Grade display:** Large grade letter (A-F) centered, colored per grade, with 1-line message below. Composite score shown as percentage.

    2. **Debt Gauge (SVG):** Circular half-arc gauge showing debt score (0-100). Debt score = inverse of composite (debtScore = Math.round((1 - composite) * 100)). Use the pure SVG pattern from RESEARCH.md: half-circle arc with strokeDasharray/strokeDashoffset, color transitions from green (<30) to gold (<60) to orange (<80) to red. Size: 160x100 viewBox. Label "DEBT SCORE" below value.

    3. **B1/B2/R1 Loop Diagram (SVG):** Post-game only (not shown during gameplay). Show the shifting-the-burden archetype diagram with 3 loops:
       - B1 (Symptomatic): Problem Symptom -> Quick Fix -> Symptom Relief -> (loop back, balancing)
       - B2 (Fundamental): Problem Symptom -> Fundamental Solution -> Root Cause Fix -> (loop back, balancing, delayed)
       - R1 (Erosion): Quick Fix -> Side Effects -> Erodes Fundamental Capacity -> (reinforcing)
       Highlight which loops were active based on student's choices: if >50% symptomatic choices, B1+R1 glow bright; if >50% fundamental, B2 glows; if mixed, all dim. Use SVG with positioned text nodes and curved path arrows. Accent color for active loops, muted for inactive.

    4. **Erosion Channel Diagnosis:** For each of the 3 named channels, show:
       - Channel name (bold)
       - 1-line explanation
       - AI-specific example from the scenario
       - Active/inactive indicator based on game metrics:
         - Knowledge Drain: active if symptomatic choices >= 6 AND fundamental choices <= 2
         - Complexity Creep: active if fixDependency > 60
         - Normalization: active if orgWill < 40 AND symptomSeverity is stable (last 3 rounds within +/-5)
       Active channels: accent border + full opacity. Inactive: muted border + 50% opacity.

    **Optimal path comparison (2nd attempt only):**
    When attemptNumber >= 2, show an additional section below the erosion channels:
    - "Optimal Strategy" header
    - Side-by-side comparison: "Your Result" vs "Optimal" for each metric
    - Show optimal path sequence as small icons (S=symptomatic, F=fundamental, T=transition, N=nothing)
    - Difference indicators: green if student beat optimal on a metric, red if worse

    **Boundaries tab contents:**
    3 counter-patterns displayed as expandable cards (from RESEARCH.md COUNTER_PATTERNS):
    1. Novel Problem -- "No known fundamental solution exists yet" -- AI example about first multimodal agent deployment
    2. Legitimate Triage -- "Quick fix IS the right call under true emergency" -- AI example about production LLM hallucinating during peak traffic
    3. Acceptable Trade-off -- "Cost of fundamental fix exceeds benefit" -- AI example about 200ms latency on 2% of requests

    Each card shows: name (bold, accent color), principle (1 line), AI example (italic), diagnostic test question.

    Below the 3 cards: **3-question mini-quiz.**
    Quiz questions (multiple choice, 3 options each):
    Q1: "An AI agent crashes on a new input type no one has seen before. The team adds an exception handler. Is this burden shifting?" -> Answer: No -- Novel Problem (no known fundamental solution yet)
    Q2: "A production chatbot starts generating offensive responses during a product launch. The team adds a content filter. Is this burden shifting?" -> Answer: No -- Legitimate Triage (emergency with planned root-cause follow-up)
    Q3: "For 6 months, a team has been adding retry logic to handle API timeouts instead of fixing the API. No one remembers why it was slow. Is this burden shifting?" -> Answer: Yes -- Knowledge Drain + Normalization (quick fix became permanent, team forgot the root cause)

    Quiz UI: show questions one at a time, student selects answer, immediate feedback (correct/incorrect with explanation), advance to next. Score shown at end (X/3).

    **Try Again button:**
    Show "Try Again" button in post-game results. On click:
    - Reset sim state via SimEngine(scenario)
    - Increment attemptNumber
    - Set phase back to 'playing'
    - Keep same scenario selected
    - On 2nd+ attempt completion, optimal path comparison section appears

    **File size check:** Keep total under 850 lines. Compress by: sharing style objects (S.* pattern from Phase 8), keeping scenario descriptions terse, reusing color constants.
  </action>
  <verify>
    <automated>python -c "
content = open('Interactive Artifact for Cubelets/automation-debt-simulator.jsx', encoding='utf-8').read()
checks = {
    'DebtGauge': 'DebtGauge' in content or 'debtGauge' in content or 'DEBT SCORE' in content,
    'LoopDiagram': 'LoopDiagram' in content or 'B1' in content,
    'erosion_channels': 'Knowledge Drain' in content and 'Complexity Creep' in content and 'Normalization' in content,
    'boundaries_tab': 'boundaries' in content.lower() and 'Novel Problem' in content,
    'counter_patterns': 'Legitimate Triage' in content and 'Acceptable Trade' in content,
    'quiz': 'quiz' in content.lower(),
    'try_again': 'Try Again' in content or 'tryAgain' in content,
    'optimal_path': 'optimal' in content.lower(),
    'activeTab': 'activeTab' in content,
    'grade_display': 'getGrade' in content or 'grade' in content,
}
passed = sum(v for v in checks.values())
for k, v in checks.items():
    print(f'  {k}: {\"PASS\" if v else \"FAIL\"}')
lines = len(content.split('\n'))
print(f'Total lines: {lines}')
print(f'Under 850 lines: {\"YES\" if lines <= 850 else \"NO -- needs compression\"}')
print(f'Result: {passed}/{len(checks)} checks passed')
print('OK' if passed >= 8 and lines <= 900 else 'FAIL')
" 2>&1</automated>
  </verify>
  <done>Post-game results screen has diagnosis tab (grade, debt gauge, B1/B2/R1 diagram, erosion channels) and boundaries tab (3 counter-patterns, 3-question quiz). Try Again button resets game. Optimal path comparison appears on 2nd attempt. Total file size under 850 lines.</done>
</task>

</tasks>

<verification>
- Artifact renders in Claude.ai sandbox (file drop test)
- 3 scenarios selectable, each plays through 12 rounds
- 2x2 dashboard updates each round with sparklines
- Choice cards show trade-off previews
- Post-game shows grade, debt gauge, B1/B2/R1 diagram, erosion channels
- Boundaries tab has 3 counter-patterns and working quiz
- Try Again resets game, optimal path shown on 2nd attempt
- Electric violet accent (#7c5cfc) used throughout, not orange
- File under 850 lines
</verification>

<success_criteria>
- Student can play complete 12-round game in all 3 scenarios
- Post-game diagnosis correctly grades using 4-metric composite
- Archetype boundaries tab educates on when burden-shift does NOT apply
- Replay mechanics work with optimal path comparison on 2nd attempt
- Artifact respects sandbox constraints (array state, z-index, token limit)
</success_criteria>

<output>
After completion, create `.planning/phases/10-st-006-automation-debt/10-01-SUMMARY.md`
</output>
