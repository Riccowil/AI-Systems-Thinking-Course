---
phase: 08-st-004-pathfinder
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx
autonomous: true
requirements: [AF-02, AF-03, AF-10]

must_haves:
  truths:
    - "Student can place 5 agent-specific node types (agent/hexagon, tool/rectangle, memory/cylinder, evaluator/diamond, constraint/octagon) on the CLD canvas"
    - "Student can draw causal links between nodes with polarity (+/-) and the artifact auto-detects reinforcing/balancing loops"
    - "Worked example (retry storm) is pre-loaded with 5-6 nodes and 2 annotated loops"
    - "Loops are highlighted with pulse animation and severity scores are computed"
    - "Collapsible primer panel at the top of the right panel provides agent basics with ST-001 cross-reference"
  artifacts:
    - path: "Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx"
      provides: "Complete agent CLD builder with shape-coded nodes, DFS cycle detection, dagre layout, worked example, and primer panel"
      min_lines: 600
  key_links:
    - from: "agent-feedback-loop-builder.jsx"
      to: "feedback-loop-builder.jsx"
      via: "Reuses findCycles() DFS algorithm and classifyLoop() function"
      pattern: "findCycles|classifyLoop"
    - from: "renderNodeShape()"
      to: "agent-visual-vocabulary.md"
      via: "SVG polygon/rect/path specs for 5 node types"
      pattern: "polygon|hexagon|cylinder|diamond|octagon"
    - from: "PRELOADED_EXAMPLE"
      to: "Retry storm scenario from CONTEXT.md"
      via: "5-6 nodes with 2 loops (reinforcing + balancing)"
      pattern: "Retry|Rate Limit|Timeout"
---

<objective>
Build the core interactive JSX artifact for ST-004: the Agent Feedback Loop Builder. This extends the ST-001 feedback-loop-builder.jsx architecture with shape-coded nodes, dagre auto-layout, the retry storm worked example, and the primer panel.

Purpose: This is the interactive artifact students use to map agent architectures as causal loop diagrams with agent-specific vocabulary. It's the visual learning tool that makes abstract agent dynamics tangible.

Output: agent-feedback-loop-builder.jsx -- a complete React artifact with agent-specific node shapes, canvas interaction, loop detection, worked example, and primer panel. Progressive disclosure and tabbed right panel are added in Plan 08-04.
</objective>

<execution_context>
@C:/Users/ricco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ricco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory/.planning/PROJECT.md
@C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory/.planning/ROADMAP.md
@C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory/.planning/STATE.md
@C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory/.planning/phases/07-content-foundations/agent-visual-vocabulary.md
@C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory/.planning/phases/07-content-foundations/agent-experience-checkpoint.md
@C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory/.planning/phases/08-st-004-pathfinder/08-CONTEXT.md
@C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory/.planning/phases/08-st-004-pathfinder/08-RESEARCH.md
@C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory/Interactive Artifact for Cubelets/feedback-loop-builder.jsx

<interfaces>
<!-- Reuse patterns from feedback-loop-builder.jsx (943 lines) -->

From Interactive Artifact for Cubelets/feedback-loop-builder.jsx:
```javascript
// PRELOADED_EXAMPLE pattern (retry storm replaces this):
const PRELOADED_EXAMPLE = {
  nodes: [
    { id: "n1", label: "Agent Complexity", x: 380, y: 80 },
    // ...
  ],
  edges: [
    { from: "n1", to: "n2", polarity: "+" },
    // ...
  ],
};

// Color scheme (reuse for consistency):
const COLORS = {
  bg: "#0f1117", canvas: "#161922", gridLine: "#1e2230",
  node: "#1c2033", nodeBorder: "#2a3050", nodeHover: "#242b45",
  nodeSelected: "#00d4aa", edgePlus: "#00d4aa", edgeMinus: "#ff6b6b",
  textPrimary: "#e8ecf4", textSecondary: "#7a839e", textMuted: "#4a5270",
  accent: "#00d4aa", accentWarm: "#ffb347", accentDanger: "#ff6b6b",
  panel: "#181c28", panelBorder: "#242940",
  reinforcing: "#00d4aa", balancing: "#6b8aff", pulse: "#00d4aa",
};

// DFS cycle detection (reuse exactly):
function findCycles(nodes, edges) { /* ... 34 lines ... */ }
function classifyLoop(cycle) { /* ... returns { type, label, negCount } */ }

// Layout: 3-panel (toolbar 72px | canvas flex | right panel 260px)
// Interaction modes: add, connect, move, delete
// Edge rendering: quadratic bezier with auto-routing
// Pulse animation: 3x repeat, 1.5s duration on loop edges
```

From agent-visual-vocabulary.md (Phase 7 specs):
```
| Type       | Shape     | Fill      | Stroke    | Size (W x H) |
|------------|-----------|-----------|-----------|---------------|
| Agent      | Hexagon   | #1a2744   | #6b8aff   | 80 x 70       |
| Tool       | Rectangle | #1a3a2e   | #00d4aa   | 90 x 50       |
| Memory     | Cylinder  | #2a1a3a   | #9b6bff   | 60 x 70       |
| Evaluator  | Diamond   | #3a2a1a   | #ffb86b   | 70 x 70       |
| Constraint | Octagon   | #3a1a1a   | #ff6b6b   | 70 x 70       |

SVG paths:
- Hexagon:  <polygon points="40,0 80,17.5 80,52.5 40,70 0,52.5 0,17.5" />
- Rectangle: <rect width="90" height="50" rx="6" ry="6" />
- Cylinder:  <path d="M0,10 C0,0 60,0 60,10 L60,60 C60,70 0,70 0,60 Z" />
- Diamond:   <polygon points="35,0 70,35 35,70 0,35" />
- Octagon:   <polygon points="20,0 50,0 70,20 70,50 50,70 20,70 0,50 0,20" />
```

From CONTEXT.md (worked example design):
- Retry storm: 5-6 nodes (Agent, Tool, Rate Limiter, Timeout, Error Handler, optional Memory)
- Two loops: reinforcing (retry escalation) + balancing (rate limiting dampening)
- Pre-loaded with annotations, pulse animation, severity in right panel, polarity labels
- Student sees complete analysis before building their own
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build agent-feedback-loop-builder.jsx with shape-coded nodes and canvas</name>
  <files>Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx</files>
  <action>
    Create agent-feedback-loop-builder.jsx as a new file extending the feedback-loop-builder.jsx architecture. Do NOT modify the existing feedback-loop-builder.jsx.

    **Structure:** This file should be a complete standalone React component (same pattern as existing artifacts -- no external imports except React hooks).

    **1. COLORS constant:** Copy from feedback-loop-builder.jsx and extend with shape-specific colors:
    ```javascript
    // Add to existing COLORS:
    agentFill: "#1a2744", agentStroke: "#6b8aff",
    toolFill: "#1a3a2e", toolStroke: "#00d4aa",
    memoryFill: "#2a1a3a", memoryStroke: "#9b6bff",
    evaluatorFill: "#3a2a1a", evaluatorStroke: "#ffb86b",
    constraintFill: "#3a1a1a", constraintStroke: "#ff6b6b",
    ```

    **2. SHAPE_SPECS constant:** Lookup table for rendering each node type:
    ```javascript
    const SHAPE_SPECS = {
      agent: { width: 80, height: 70, label: "Agent", /* SVG polygon points */ },
      tool: { width: 90, height: 50, label: "Tool", /* rect with rx=6 */ },
      memory: { width: 60, height: 70, label: "Memory", /* cylinder path */ },
      evaluator: { width: 70, height: 70, label: "Evaluator", /* diamond points */ },
      constraint: { width: 70, height: 70, label: "Constraint", /* octagon points */ },
    };
    ```
    Use exact SVG path data from agent-visual-vocabulary.md.

    **3. PRELOADED_EXAMPLE:** Retry storm worked example with 5-6 nodes and 2 loops per CONTEXT.md:
    - Nodes: Agent (hexagon), Tool (rectangle), Rate Limiter (constraint/octagon), Timeout (constraint/octagon), Error Handler (evaluator/diamond), optionally Memory (cylinder)
    - Each node has: id, label, component_type, x, y
    - Reinforcing loop: Agent -> Tool -> Error Handler -> Agent (all + polarity, retry escalation)
    - Balancing loop: Agent -> Tool -> Rate Limiter -> Agent (+ then - polarity, dampening)
    - Edges have: from, to, polarity, strength (0.0-1.0), label
    - Use dagre to compute initial x/y positions or pre-compute them manually for clean layout
    - Include annotations for pre-loaded mode: which edges belong to which loops

    **4. Node rendering (renderNodeShape function):**
    - Accept node object with component_type field
    - Render the appropriate SVG shape per SHAPE_SPECS
    - Apply fill/stroke colors from COLORS based on component_type
    - Selected state: stroke width 2px -> 3px, subtle glow
    - Hover state: fill opacity 0.8 -> 1.0, cursor pointer
    - Drag state: opacity 0.6
    - Label centered inside shape, DM Sans 12px, #e8ecf4, truncated with ellipsis if > shape width - 16px

    **5. Left toolbar (72px):**
    - 5 node type buttons with mini SVG shape icons (small version of each shape, ~24x24)
    - "Connect" mode button (existing pattern)
    - "Move" mode button (existing pattern)
    - "Delete" mode button (existing pattern)
    - "Auto-arrange" button (triggers dagre layout)
    - "Load Example" / "Clear" toggle button
    - Active mode highlighted with accent color

    **6. Canvas (center, flex):**
    - SVG canvas on #0f1117 background with subtle grid dots (#1e2130)
    - Click to place node of selected type
    - Drag to move nodes
    - Connect mode: click source, click target, prompt for polarity (+/-) and optional strength
    - Edge rendering: quadratic bezier curves with polarity labels (reuse pattern from feedback-loop-builder.jsx)
    - Pulse animation on edges that belong to detected loops (3x repeat, 1.5s duration)
    - Arrow heads on edges (filled triangle at target)

    **7. findCycles and classifyLoop:** Copy from feedback-loop-builder.jsx -- these are the proven DFS algorithms. No changes needed except nodes now have component_type field (findCycles only uses id, so no code change required).

    **8. Loop severity scoring (JavaScript, matches MCP tool):**
    ```javascript
    function scoreLoopSeverity(cycle, edges) {
      let gain = 1.0;
      for (let i = 0; i < cycle.length - 1; i++) {
        const edge = edges.find(e => e.from === cycle[i].node && e.to === cycle[i+1].node);
        gain *= (edge?.strength || 1.0);
      }
      const score = Math.round(gain * 100);
      const clamped = Math.min(100, Math.max(0, score));
      const label = clamped <= 33 ? 'Low' : clamped <= 66 ? 'Medium' : 'High';
      return { score: clamped, label };
    }
    ```

    **9. Right panel (280px) -- basic structure for this plan:**
    - Panel background #181c28, border-left 1px #242940
    - For THIS plan, render a simple "Loops" section showing detected loops with:
      - Loop ID (R1, R2, B1, B2)
      - Path (node names joined with arrows)
      - Type (Reinforcing/Balancing)
      - Severity score and label
    - Collapsible primer panel at the TOP (accordion, collapsed by default):
      - Title: "Agent Basics" with expand/collapse chevron
      - Content per agent-experience-checkpoint.md ST-004 primer:
        - What is an AI agent (vs chatbot)
        - What are tools, memory, evaluators, constraints
        - Cross-reference link text: "Review ST-001: Reinforcing Feedback Loops for loop polarity basics"
    - The tabbed interface (Loops/Predictions/Interventions tabs) is added in Plan 08-04

    **10. Dagre integration:**
    - Import dagre via: `import dagre from '@dagrejs/dagre'` -- but since this is a standalone React artifact that runs in Claude's artifact sandbox, dagre must be included inline OR the auto-arrange feature should use a simple force-directed layout approximation if dagre is not available in the sandbox.
    - FALLBACK: If dagre import fails (likely in artifact sandbox), implement a simple top-to-bottom layout algorithm: rank nodes by topological order, space vertically by 100px, horizontally center within rank. This is sufficient for 5-6 node graphs.
    - Auto-arrange button triggers layout recalculation
    - Layout only runs on explicit button press (never auto-triggers -- per pitfall #2)

    **11. Two modes:**
    - "Example" mode: pre-loaded retry storm with all annotations visible, loops highlighted
    - "Practice" mode: empty canvas, student builds their own diagram
    - Toggle via "Load Example" / "Clear" button in toolbar
    - In Example mode, nodes are still draggable but not deletable

    **Important styling notes:**
    - ALL styles must be inline (no external CSS) -- this is a React artifact
    - Font: DM Sans for labels, JetBrains Mono for data/scores
    - Dark cybernetic theme matching existing artifacts
    - Limit canvas to 20 nodes max (show warning toast if exceeded)
  </action>
  <verify>
    <automated>cd "C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory" && python -c "
content = open('Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx', 'r', encoding='utf-8').read()
# Check core elements exist
checks = {
    'React hooks': 'useState' in content and 'useRef' in content,
    'findCycles': 'findCycles' in content,
    'classifyLoop': 'classifyLoop' in content,
    'PRELOADED_EXAMPLE': 'PRELOADED_EXAMPLE' in content,
    'SHAPE_SPECS or shapes': 'SHAPE_SPECS' in content or 'shapeSpecs' in content or 'agent.*hexagon' in content.lower(),
    'component_type': 'component_type' in content,
    '5 node types': all(t in content for t in ['agent', 'tool', 'memory', 'evaluator', 'constraint']),
    'hexagon SVG': '40,0' in content and '80,17.5' in content,
    'severity scoring': 'severity' in content.lower(),
    'primer panel': 'primer' in content.lower() or 'Agent Basics' in content,
    'polarity': 'polarity' in content,
    'loop detection': 'reinforcing' in content and 'balancing' in content,
    'retry storm': 'retry' in content.lower() or 'Retry' in content,
}
for check, passed in checks.items():
    status = 'PASS' if passed else 'FAIL'
    print(f'  {status}: {check}')
    assert passed, f'Failed check: {check}'
lines = len(content.split(chr(10)))
print(f'Total lines: {lines}')
assert lines >= 600, f'Expected >= 600 lines, got {lines}'
print('Artifact core OK')
"</automated>
  </verify>
  <done>agent-feedback-loop-builder.jsx is a complete standalone React artifact with 5 shape-coded node types, DFS loop detection, severity scoring, worked retry storm example, primer panel, canvas interaction (place, connect, move, delete), and auto-arrange layout. Right panel shows detected loops with IDs, types, and severity.</done>
</task>

</tasks>

<verification>
1. File exists and has >= 600 lines
2. All 5 node types render with correct SVG shapes per visual vocabulary
3. findCycles and classifyLoop are present (reused from feedback-loop-builder.jsx)
4. PRELOADED_EXAMPLE has 5-6 nodes with 2 loops (retry storm)
5. Severity scoring produces 0-100 scores with Low/Medium/High labels
6. Primer panel is collapsible at top of right panel
7. Loops panel shows R1/R2/B1/B2 classifications
8. Two modes: Example (pre-loaded) and Practice (empty canvas)
</verification>

<success_criteria>
- agent-feedback-loop-builder.jsx renders in React artifact sandbox
- 5 distinct SVG shapes render with correct colors per agent-visual-vocabulary.md
- DFS cycle detection finds loops correctly (even negatives = reinforcing)
- Retry storm example loads with 2 annotated loops
- Severity = loop gain * 100, clamped 0-100, labeled Low/Medium/High
- Primer panel is collapsible accordion with agent basics content
- Canvas supports: place node, connect edges, move, delete, auto-arrange
</success_criteria>

<output>
After completion, create `.planning/phases/08-st-004-pathfinder/08-03-SUMMARY.md`
</output>
