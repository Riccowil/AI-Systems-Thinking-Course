---
phase: 08-st-004-pathfinder
plan: 04
type: execute
wave: 2
depends_on: [08-03]
files_modified:
  - Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx
autonomous: false
requirements: [AF-04, AF-05]

must_haves:
  truths:
    - "Student predicts loop type (reinforcing/balancing) AND behavior (grows/stabilizes/oscillates) via dropdown BEFORE algorithmic analysis is revealed"
    - "Artifact does NOT show loop analysis until student commits predictions for all detected loops"
    - "After reveal, right panel shows side-by-side comparison: student prediction vs actual with green check / red X"
    - "Right panel has 3 tabs: Loops, Predictions, Interventions"
    - "High-severity loops show auto-generated intervention suggestions identifying highest-gain link"
    - "Two modes work: example (pre-analyzed, study) vs practice (build, predict, reveal)"
  artifacts:
    - path: "Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx"
      provides: "Progressive disclosure UX, tabbed right panel, prediction comparison, intervention suggestions"
      contains: "predictions"
  key_links:
    - from: "predictions state"
      to: "revealedLoops state"
      via: "handlePredictionSubmit gates reveal of algorithmic analysis"
      pattern: "predictions.*revealedLoops|handlePredictionSubmit"
    - from: "Interventions tab"
      to: "scoreLoopSeverity()"
      via: "High-severity loops (67-100) get auto-generated suggestions"
      pattern: "severity.*High|intervention"
    - from: "Practice mode"
      to: "Prediction form"
      via: "Predictions only appear in practice mode (not example mode)"
      pattern: "mode.*practice|isPracticeMode"
---

<objective>
Add progressive disclosure (predict-then-reveal) and the tabbed right panel (Loops/Predictions/Interventions) to the agent-feedback-loop-builder.jsx artifact.

Purpose: Progressive disclosure is the pedagogical core of ST-004 -- the "moment of truth" where students commit a prediction and then see if their mental model matches the algorithm's analysis. The tabbed panel organizes loop data, predictions, and interventions cleanly.

Output: Updated agent-feedback-loop-builder.jsx with complete progressive disclosure UX and tabbed right panel.
</objective>

<execution_context>
@C:/Users/ricco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ricco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory/.planning/PROJECT.md
@C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory/.planning/ROADMAP.md
@C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory/.planning/STATE.md
@C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory/.planning/phases/08-st-004-pathfinder/08-CONTEXT.md
@C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory/.planning/phases/08-st-004-pathfinder/08-RESEARCH.md
@C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory/.planning/phases/08-st-004-pathfinder/08-03-SUMMARY.md
@C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory/Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx

<interfaces>
<!-- Plan 08-03 creates the artifact. This plan modifies it. Key state to work with: -->

Expected state from Plan 08-03 in agent-feedback-loop-builder.jsx:
```javascript
// Nodes have component_type field:
{ id: "n1", label: "Agent", component_type: "agent", x: 380, y: 80 }

// Loops detected via findCycles() and classified via classifyLoop():
// Each classified loop: { type: "reinforcing"|"balancing", label: "R1"|"B1", path: [...], negCount: N }

// Severity scored via scoreLoopSeverity():
// Returns: { score: 0-100, label: "Low"|"Medium"|"High" }

// Right panel already shows basic loops list (no tabs yet)
// Primer panel already collapsible at top

// Two modes: Example vs Practice
// PRELOADED_EXAMPLE with retry storm (5-6 nodes, 2 loops)
```

From CONTEXT.md (progressive disclosure spec):
```
- Student predicts loop type (reinforcing vs balancing) AND behavior (grows, stabilizes, oscillates) via simple dropdown per detected loop
- Prediction required before algorithmic analysis is revealed
- Reveal: right panel animates in the algorithmic analysis next to student's prediction with comparison (green check / red X per loop)
- Two modes: worked example (pre-analyzed, student studies it) vs practice mode (student builds, predicts, then reveals)
```

From CONTEXT.md (right panel spec):
```
- 3 tabs: Loops, Predictions, Interventions
- Primer panel is collapsible accordion at TOP (above tabs)
- Right panel width: ~280px
- Tab styling: follow existing JetBrains Mono tab pattern
```

From RESEARCH.md (state management pattern):
```javascript
const [predictions, setPredictions] = useState({}); // { loopId: { type, behavior } }
const [revealedLoops, setRevealedLoops] = useState(new Set());

const handlePredictionSubmit = (loopId) => {
  setRevealedLoops(prev => new Set([...prev, loopId]));
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tabbed right panel and progressive disclosure state management</name>
  <files>Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx</files>
  <action>
    Modify agent-feedback-loop-builder.jsx to replace the basic loops list in the right panel with a tabbed interface and add progressive disclosure state management.

    **1. New state variables:**
    ```javascript
    const [activeTab, setActiveTab] = useState('loops'); // 'loops' | 'predictions' | 'interventions'
    const [predictions, setPredictions] = useState({}); // { loopId: { type: 'reinforcing'|'balancing', behavior: 'grows'|'stabilizes'|'oscillates' } }
    const [revealedLoops, setRevealedLoops] = useState(new Set());
    const [allPredicted, setAllPredicted] = useState(false);
    ```

    **2. Tab bar (below primer panel, above tab content):**
    - 3 tabs: "Loops", "Predictions", "Interventions"
    - Styled with JetBrains Mono 11px, uppercase
    - Active tab: teal (#00d4aa) bottom border, lighter text
    - Inactive tabs: muted text (#7a839e), hover brightens
    - Tab bar background: #181c28 matching panel

    **3. Loops tab content:**
    - Same loop list as Plan 08-03 created, but move it into the tab container
    - For each detected loop: ID (R1/B1), path with arrows, type, severity score + label
    - If in practice mode AND loops not yet revealed: show loop IDs and paths but HIDE type and severity (replaced with "?" placeholders and message "Submit predictions to reveal analysis")
    - If in example mode: show everything (pre-analyzed)

    **4. Predictions tab content:**
    - Only active in practice mode. In example mode, show: "Predictions are for practice mode. Switch to practice to test yourself."
    - In practice mode, for each detected loop:
      - Loop ID and path (visible)
      - Dropdown 1: "Loop type" with options: Reinforcing, Balancing
      - Dropdown 2: "Behavior" with options: Grows exponentially, Stabilizes, Oscillates
      - Styled: dark select inputs (#1c2033 bg, #e8ecf4 text, #242940 border)
    - "Submit All Predictions" button (disabled until all loops have both dropdowns filled)
    - Button styled: teal accent (#00d4aa) bg when active, muted when disabled
    - On submit: setAllPredicted(true), reveal all loops at once

    **After reveal (allPredicted === true):**
    - Each loop shows side-by-side comparison:
      - "Your prediction: Reinforcing, Grows" vs "Actual: Reinforcing, Grows"
      - Green checkmark icon (inline SVG, teal color) if type matches, red X (coral) if wrong
      - Behavior comparison: green check if correct, amber X if wrong
    - Summary: "You got N/M loop types correct and N/M behaviors correct"
    - "Reset Predictions" button to try again

    **5. Interventions tab content:**
    - For each loop with severity "High" (67-100):
      - Loop ID and path
      - "Highest-gain link: {from} -> {to} (strength: {strength})"
      - Suggestion: "Consider adding rate limiting between {from} and {to} to reduce coupling" or similar per loop context
      - Use amber (#ffb86b) accent for intervention text
    - For Medium severity: "Monitor this loop -- it may escalate under load"
    - For Low severity: "This loop is within acceptable bounds"
    - If no loops detected: "Build your agent diagram to detect loops"
    - If in practice mode AND not yet revealed: "Submit predictions to see interventions"

    **6. Graph structure change handling (per pitfall #3):**
    - When nodes or edges are added/removed (graph structure changes):
      - Clear predictions: `setPredictions({})`
      - Clear revealed: `setRevealedLoops(new Set())`
      - Set allPredicted to false
      - Show brief toast: "Graph changed -- predictions reset"
    - This prevents orphaned predictions keyed to old loop IDs

    **7. Example mode vs Practice mode behavior:**
    - Example mode: All tabs fully populated, no prediction UI, all analysis visible. Loops tab shows all data. Predictions tab shows "Predictions are for practice mode." Interventions tab shows full intervention analysis.
    - Practice mode: Loops tab hides type/severity until predicted. Predictions tab shows dropdown forms. Interventions tab shows analysis only after predictions submitted.

    **8. Animation on reveal:**
    - When predictions are submitted and analysis appears, use a subtle CSS transition:
      - opacity 0 -> 1 over 300ms
      - translateY(4px) -> translateY(0) over 300ms
    - Apply to the comparison view and the interventions tab content
    - Use inline style with transition property (no external CSS)
  </action>
  <verify>
    <automated>cd "C:/Users/ricco/Documents/Wilson Consulting/obsidian-vault/Divergence Academy/Course factory" && python -c "
content = open('Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx', 'r', encoding='utf-8').read()
checks = {
    'tabs state': 'activeTab' in content,
    'predictions state': 'predictions' in content and 'setPredictions' in content,
    'revealedLoops state': 'revealedLoops' in content or 'allPredicted' in content,
    'three tab names': all(t in content for t in ['Loops', 'Predictions', 'Interventions']),
    'prediction dropdowns': ('Reinforcing' in content or 'reinforcing' in content) and ('Balancing' in content or 'balancing' in content),
    'behavior options': any(b in content for b in ['grows', 'Grows', 'stabilizes', 'Stabilizes', 'oscillates', 'Oscillates']),
    'submit predictions': 'submit' in content.lower() or 'Submit' in content,
    'comparison view': ('check' in content.lower() or 'correct' in content.lower()) and ('predicted' in content.lower() or 'prediction' in content.lower()),
    'intervention suggestions': 'intervention' in content.lower(),
    'severity High': 'High' in content,
    'graph change reset': 'reset' in content.lower() or 'clear' in content.lower(),
    'example vs practice': ('example' in content.lower() or 'Example' in content) and ('practice' in content.lower() or 'Practice' in content),
}
for check, passed in checks.items():
    status = 'PASS' if passed else 'FAIL'
    print(f'  {status}: {check}')
    assert passed, f'Failed check: {check}'
lines = len(content.split(chr(10)))
print(f'Total lines: {lines}')
assert lines >= 800, f'Expected >= 800 lines after adding progressive disclosure, got {lines}'
print('Progressive disclosure + tabs OK')
"</automated>
  </verify>
  <done>Right panel has 3 tabs (Loops/Predictions/Interventions), progressive disclosure requires prediction before analysis reveal, comparison shows green check / red X, interventions suggest weakening highest-gain links, graph changes reset predictions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete ST-004 three-layer stack</name>
  <files>Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx</files>
  <action>
    Human verifies the complete ST-004 stack: artifact with progressive disclosure, MCP tool, cubelet markdown, and Claude skill.

    Steps to verify:
    1. Open agent-feedback-loop-builder.jsx in the Claude artifact preview (paste contents into Claude.ai chat and ask to render it)
    2. Verify the retry storm example loads with 5-6 nodes showing distinct shapes (hexagon for Agent, rectangle for Tool, etc.)
    3. Verify loops are highlighted with pulse animation
    4. Switch to Practice mode -- canvas should clear
    5. Place at least 3 nodes of different types and connect them with edges
    6. Check that the Predictions tab shows dropdowns for each detected loop
    7. Submit predictions and verify the comparison appears with green/red indicators
    8. Check that the Interventions tab shows suggestions for high-severity loops
    9. Verify the primer panel is collapsible at the top of the right panel
    10. Open ST-004-agent-feedback-loops.md and verify all 6 faces are present with scores
    11. Verify agent-feedback-analyzer.skill is a valid ZIP (can be opened)
  </action>
  <verify>Human confirms all 11 verification steps pass</verify>
  <done>Complete ST-004 three-layer stack approved by human: artifact renders correctly with progressive disclosure, cubelet has 6 faces, skill ZIP is valid</done>
</task>

</tasks>

<verification>
1. Three tabs render in right panel (Loops, Predictions, Interventions)
2. Progressive disclosure: predictions must be submitted before analysis reveals
3. Comparison shows student prediction vs actual with visual indicators
4. Interventions auto-generated for high-severity loops
5. Graph changes reset predictions (no orphaned state)
6. Example mode shows everything; Practice mode gates behind predictions
7. Full three-layer stack (artifact + MCP tool + skill + markdown) verified
</verification>

<success_criteria>
- Tabbed right panel with 3 tabs renders correctly
- Progressive disclosure works: no analysis until predictions submitted
- Prediction comparison shows green check / red X per loop
- Interventions show for high-severity loops with highest-gain link identified
- Example mode is pre-analyzed; Practice mode requires predictions
- Human verifies the complete ST-004 stack visually
</success_criteria>

<output>
After completion, create `.planning/phases/08-st-004-pathfinder/08-04-SUMMARY.md`
</output>
