---
phase: 09-st-005-tool-orchestration
plan: 04
type: execute
wave: 2
depends_on: [03]
files_modified:
  - Interactive Artifact for Cubelets/tool-orchestration-analyzer.jsx
autonomous: false
requirements: [TO-02, TO-03]

must_haves:
  truths:
    - "Student can click 'break' icon on any tool to simulate failure -- tool turns red, downstream cascade shows graduated colors"
    - "Blast radius highlight shows direct dependents in bright coral, transitive dependents dimmer with fading intensity"
    - "Redundancy is highlighted with overlap badge around tool pairs with matching I/O overlap"
    - "Student can add interventions and predict Meadows level BEFORE system reveals actual level"
    - "Before/after health score comparison shows delta when intervention is added"
    - "Artifact renders correctly in Claude.ai artifact sandbox"
  artifacts:
    - path: "Interactive Artifact for Cubelets/tool-orchestration-analyzer.jsx"
      provides: "Enhanced artifact with failure simulation, blast radius, redundancy viz, intervention scoring with progressive disclosure"
      min_lines: 300
  key_links:
    - from: "failure simulation"
      to: "graduated cascade"
      via: "BFS from failed tool following edges, color by dependency type"
      pattern: "failedTools|cascade|degraded"
    - from: "intervention scoring"
      to: "Meadows hierarchy"
      via: "prediction vs actual comparison with visual feedback"
      pattern: "predict|meadows|comparison"
    - from: "intervention"
      to: "health scores"
      via: "before/after delta calculation"
      pattern: "delta|projected|before.*after"
---

<objective>
Enhance the tool-orchestration-analyzer.jsx artifact with failure simulation mode (graduated cascade visualization), blast radius highlighting, redundancy detection visualization, and intervention scoring with progressive disclosure (predict Meadows level before reveal). Then verify it renders in Claude.ai sandbox.

Purpose: Complete the interactive learning experience. Failure simulation teaches that dependency TYPE determines blast severity. Progressive disclosure of intervention Meadows levels is the core pedagogical moment ("Is removing a tool a parameter tweak or a structural change?"). Compression ensures sandbox compliance.
Output: Final artifact with all interactive features, verified in sandbox
</objective>

<execution_context>
@C:/Users/ricco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ricco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-st-005-tool-orchestration/09-CONTEXT.md
@.planning/phases/09-st-005-tool-orchestration/09-RESEARCH.md
@.planning/phases/09-st-005-tool-orchestration/09-03-SUMMARY.md
@.planning/phases/08-st-004-pathfinder/08-04-SUMMARY.md
@.planning/phases/08-st-004-pathfinder/08-05-SUMMARY.md

<interfaces>
<!-- From Plan 03 artifact (will exist when this plan runs) -->

Expected state from Plan 03:
```jsx
// State variables to extend:
// failedTools (Set) -- already exists from worked example pre-fail
// selectedTool -- already exists for tool selection
// healthScores -- already computed

// Functions to extend:
// calculateHealthScores(nodes, edges) -- add "projected" variant for interventions
// findCycles(nodes, edges) -- already exists for cycle detection
// renderNode(tool) -- add failure simulation visual states
// renderEdge(edge) -- add cascade coloring

// Right panel tabs: Topology / Health / Interventions
// Interventions tab needs: intervention list, prediction form, comparison view
```

From .planning/phases/09-st-005-tool-orchestration/09-CONTEXT.md (locked decisions):
```
Failure Simulation Mode:
- Toggle tool failure: click 'break' icon on any tool -- tool turns red
- Graduated cascade: required=red (broken), optional=amber (degraded), enhances=green (unaffected)
- Single 'Reset' button in toolbar clears all failure states
- Students can break and reset repeatedly

Blast Radius (Analyze mode):
- Click tool to highlight downstream cascade
- Direct dependents glow bright coral
- Transitive dependents dimmer with fading intensity

Redundancy:
- Group highlight with overlap badge around tool pairs with matching I/O overlap
- "redundant" label

Intervention Scoring UX:
- Click tool in analyze mode -> right panel shows 'Add Intervention'
- Select action type: Add tool, Remove tool, Refactor dependency, Add cache/fallback
- System assigns Meadows level based on action type
- Progressive disclosure: student predicts L1-L12 BEFORE reveal
- Comparison: green check if close (within 2 levels), red X if far
- Before/after health score comparison with delta

Action type to Meadows level mapping (Claude's discretion from research):
- Add tool = L1 (parameter)
- Remove tool = L2 (buffer/stock)
- Add cache/fallback = L5 (feedback delays)
- Refactor dependency = L6 (feedback structure)
- Change system goals = L10 (goals)
```

From Phase 8 Plan 05 (compression lessons):
```
- Shared style objects (S.*) pattern saves 150-200 lines
- Rendering helpers consolidate repeated JSX patterns
- Compact JSX: single-line for <=3 properties
- Use useRef for derived state to avoid stale closures in useEffect
- No alert() -- use inline warning banners with setTimeout
- Target: 546 lines (ST-004 final), ST-005 target similar
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failure simulation, blast radius, redundancy visualization, and intervention scoring with progressive disclosure</name>
  <files>Interactive Artifact for Cubelets/tool-orchestration-analyzer.jsx</files>
  <action>
    Enhance tool-orchestration-analyzer.jsx with the remaining interactive features. Maintain compressed coding style to stay under sandbox limits.

    **1. Failure Simulation Mode (Analyze mode):**
    Add 'break' icon (small X or lightning bolt) on each tool node in Analyze mode.
    - Click break icon -> toggle tool in failedTools Set
    - Failed tool: red fill (#ff6b6b at 20% opacity), red border
    - Graduated cascade computation (computeCascade function):
      - BFS from each failed tool through edges
      - For each downstream tool, check the dependency type of the incoming edge:
        - required -> status: "broken" (red fill, red border)
        - optional -> status: "degraded" (amber fill, amber border)
        - enhances -> status: "unaffected" (no change, green tint)
      - Transitive cascades: if a "broken" tool has downstream required deps, those are also "broken"
      - Store cascade result in cascadeStatus state: Map<toolId, "broken"|"degraded"|"unaffected">
    - Reset button in toolbar: clears failedTools Set and cascadeStatus
    - Cascade recalculates whenever failedTools changes (useEffect)

    **2. Blast Radius Highlighting (Analyze mode):**
    When a tool is selected (clicked) in Analyze mode:
    - Compute blast radius: BFS downstream following all edge types
    - Direct dependents (depth 1): bright coral border (#ff6b6b at full opacity)
    - Transitive dependents (depth 2+): fading intensity (opacity = 1 / depth, min 0.3)
    - Non-affected tools: dim to 40% opacity
    - Show blast radius depth count in Health tab per-tool breakdown

    **3. Redundancy Detection Visualization:**
    Add detectRedundancy function:
    - For each pair of tools, check if they share ANY input field AND ANY output field
    - If redundant pair found: draw a subtle highlight group around both tools (dashed coral rectangle)
    - Add "redundant" badge text between the pair
    - Show redundant pairs count in Topology tab
    - Update redundancy health score based on count

    **4. Intervention Scoring with Progressive Disclosure (Interventions tab):**
    Add state: interventions (array), predictionMode (per intervention)

    When tool is selected in Analyze mode, Interventions tab shows "Add Intervention" button.
    - Click "Add Intervention" -> form appears:
      - Action type dropdown: Add tool, Remove tool, Refactor dependency, Add cache/fallback
      - Target: auto-filled with selected tool name
      - Description: text input
    - After submission, system assigns Meadows level from mapping:
      - Add tool = L1 (Constants/parameters)
      - Remove tool = L2 (Buffer/stock sizes)
      - Add cache/fallback = L5 (Feedback loop delays)
      - Refactor dependency = L6 (Feedback loop structure)
    - Progressive disclosure flow:
      1. Student sees intervention card with action type and target
      2. Student selects predicted Meadows level from L1-L12 dropdown
      3. Student clicks "Reveal" button
      4. System shows actual level side-by-side with prediction
      5. Green checkmark if within 2 levels, red X if further apart
    - Before/after health score comparison:
      - When intervention is added, compute projected health scores (simulate the graph change)
      - Show current vs projected side by side: "Brittleness: 78 -> 45 (down 33)"
      - Color the delta: green for improvement, red for worsening

    **5. Compression Check:**
    After adding all features, verify total line count stays under 550 lines.
    If over limit, apply compression:
    - Extract repeated intervention card rendering into helper
    - Collapse cascade computation into fewer lines
    - Merge shared styles further
    - Remove redundant whitespace

    If significantly over (>600 lines), consider splitting the cascade/intervention logic into compact helper functions at the top of the file.
  </action>
  <verify>
    <automated>python -c "
import os
path = 'Interactive Artifact for Cubelets/tool-orchestration-analyzer.jsx'
content = open(path, encoding='utf-8').read()
lines = len(content.split('\n'))
size = os.path.getsize(path)

checks = {
    'Failure sim (failedTools/cascade)': 'failedTools' in content or 'cascade' in content or 'broken' in content,
    'Blast radius': 'blast' in content.lower() or 'downstream' in content.lower(),
    'Redundancy detection': 'redundan' in content.lower(),
    'Intervention prediction': 'predict' in content.lower() or 'Predict' in content,
    'Meadows levels': 'meadows' in content.lower() or 'L1' in content,
    'Before/after delta': 'delta' in content.lower() or 'projected' in content.lower() or '->' in content,
    'Progressive disclosure': 'reveal' in content.lower() or 'Reveal' in content,
    'No alert()': 'alert(' not in content,
    'Under 600 lines': lines <= 600,
    'Under 55KB': size <= 55000,
}
for name, ok in checks.items():
    print(f'  {\"PASS\" if ok else \"FAIL\"}: {name}')
print(f'Lines: {lines}, Size: {size} bytes')
print(f'Overall: {\"OK\" if all(checks.values()) else \"FAIL\"}')" 2>&1</automated>
  </verify>
  <done>Artifact has failure simulation with graduated cascade, blast radius highlighting, redundancy detection, and intervention scoring with progressive disclosure (predict-then-reveal). Under sandbox limits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify artifact renders correctly in Claude.ai sandbox</name>
  <files>Interactive Artifact for Cubelets/tool-orchestration-analyzer.jsx</files>
  <action>
    Human verification of the complete ST-005 artifact in Claude.ai sandbox. This task pauses for user confirmation.

    What was built:
    - 9-tool worked example (actual course tool stack) with create_causal_loop pre-failed
    - Build/Analyze mode toggle
    - Failure simulation (click break icon, graduated cascade: required=red, optional=amber, enhances=green)
    - Blast radius highlighting (click tool, downstream cascade with fading intensity)
    - Health scores (complexity/redundancy/brittleness/aggregate) in right panel
    - Intervention scoring with progressive disclosure (predict Meadows level before reveal)
    - Before/after health score deltas
    - Primer panel referencing ST-002 concepts
    - 3 annotation callouts on canvas
  </action>
  <verify>
    Steps to verify:
    1. Open Claude.ai (claude.ai)
    2. Start a new conversation
    3. Paste the contents of: Interactive Artifact for Cubelets/tool-orchestration-analyzer.jsx
    4. Verify artifact renders (not blank white screen, not text-only)
    5. Verify 9 tools visible on canvas with 2 server background regions
    6. Verify create_causal_loop shows red (failed) with downstream cascade colors
    7. Click a different tool's break icon -- verify cascade updates
    8. Click Reset -- verify all tools return to normal
    9. Click "Start Fresh" -- verify canvas clears, switches to Build mode
    10. In Build mode: add a new tool (name + criticality), verify it appears
    11. Draw an edge between two tools using dependency type button
    12. Switch to Analyze mode -- verify health scores appear in right panel
    13. Click a tool -- verify blast radius highlighting and per-tool health breakdown
    14. Open Interventions tab -- add an intervention, predict Meadows level, click Reveal
    15. Verify green checkmark or red X comparison display
    16. Verify before/after health score delta shown
  </verify>
  <done>Artifact renders in Claude.ai sandbox, all 16 verification steps pass, user types "approved"</done>
</task>

</tasks>

<verification>
- Failure simulation: click break -> red tool + graduated cascade downstream
- Blast radius: click tool in analyze mode -> downstream tools highlighted with fading intensity
- Redundancy: tool pairs with matching I/O highlighted with badge
- Intervention scoring: predict Meadows level -> reveal -> comparison shown
- Health score deltas: before/after comparison when intervention added
- Artifact renders in Claude.ai sandbox
- Total under 550 lines / 55KB
</verification>

<success_criteria>
- All interactive features work (failure sim, blast radius, redundancy, interventions)
- Progressive disclosure is the core pedagogical moment: student predicts, then sees reality
- Before/after deltas make interventions feel concrete and measurable
- Artifact renders in Claude.ai artifact sandbox (confirmed by human verification)
</success_criteria>

<output>
After completion, create `.planning/phases/09-st-005-tool-orchestration/09-04-SUMMARY.md`
</output>
