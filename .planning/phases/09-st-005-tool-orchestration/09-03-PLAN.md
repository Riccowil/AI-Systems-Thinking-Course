---
phase: 09-st-005-tool-orchestration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Interactive Artifact for Cubelets/tool-orchestration-analyzer.jsx
  - preview-app/vitest.config.js
  - preview-app/src/__tests__/tool-orchestration-analyzer.test.jsx
  - preview-app/src/__tests__/fixtures/worked-example-tool-stack.json
autonomous: true
requirements: [TO-02, TO-04, TO-05, TO-10]

must_haves:
  truths:
    - "Student sees a worked example of the actual 9-tool course stack with create_causal_loop pre-failed on first load"
    - "Student can switch between Build mode (add tools, draw edges) and Analyze mode (blast radius, health scores)"
    - "Dependency graph renders with top-down hierarchy layout using topological sort"
    - "Right panel has 3 tabs (Topology / Health / Interventions) showing graph stats, health scores, and intervention analysis"
    - "Health scores (complexity, redundancy, brittleness, aggregate) display as 0-100 with color-coded bars"
    - "Primer panel at top of right panel cross-references ST-002 concepts"
  artifacts:
    - path: "Interactive Artifact for Cubelets/tool-orchestration-analyzer.jsx"
      provides: "Complete React artifact with dependency graph visualization, worked example, health scoring, build/analyze modes"
      min_lines: 300
    - path: "preview-app/vitest.config.js"
      provides: "Vitest configuration for artifact testing"
    - path: "preview-app/src/__tests__/tool-orchestration-analyzer.test.jsx"
      provides: "Test stubs for artifact behavior verification"
      min_lines: 40
    - path: "preview-app/src/__tests__/fixtures/worked-example-tool-stack.json"
      provides: "Fixture with 9 tools and 12 dependencies for test assertions"
  key_links:
    - from: "tool-orchestration-analyzer.jsx"
      to: "worked example data"
      via: "WORKED_EXAMPLE constant with 9 tools and dependencies"
      pattern: "WORKED_EXAMPLE|create_causal_loop"
    - from: "tool-orchestration-analyzer.jsx"
      to: "health scoring"
      via: "calculateHealthScores function"
      pattern: "calculateHealthScores|complexity|redundancy|brittleness"
    - from: "right panel"
      to: "ST-002 concepts"
      via: "primer panel content"
      pattern: "Meadows|leverage|ST-002"
---

<objective>
Build the core interactive artifact for ST-005: tool-orchestration-analyzer.jsx. This artifact renders a dependency graph of MCP tools, includes the worked example (actual course tool stack with create_causal_loop pre-failed), provides Build/Analyze modes, calculates health scores, and has a tabbed right panel with topology/health/interventions tabs plus primer panel.

Purpose: Create the interactive learning experience where students visually explore tool dependencies, blast radius, and health metrics. This is the foundation artifact -- Plan 04 adds failure simulation, blast radius highlighting, redundancy visualization, and intervention scoring with progressive disclosure.
Output: Self-contained React JSX artifact with graph rendering, worked example, health scoring, and tabbed right panel
</objective>

<execution_context>
@C:/Users/ricco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ricco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-st-005-tool-orchestration/09-CONTEXT.md
@.planning/phases/09-st-005-tool-orchestration/09-RESEARCH.md
@.planning/phases/08-st-004-pathfinder/08-03-SUMMARY.md
@.planning/phases/08-st-004-pathfinder/08-05-SUMMARY.md

<interfaces>
<!-- Patterns from ST-004 artifact to reuse -->

From Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx (architecture to mirror):
```jsx
// 3-panel layout: left toolbar (~72px) + central SVG canvas (flex) + right panel (~280px)
// COLORS object: bg=#0f1117, canvas=#161922, accent=#00d4aa (teal), accentDanger=#ff6b6b (coral), textPrimary=#e8ecf4
// SHAPE_SPECS pattern: lookup table mapping type -> render function
// findCycles: DFS with recursion stack
// autoArrangeNodes: topological sort with layer-based positioning
// Shared style objects (S.*) for compression
// Inline styles only (no external CSS)
// useState for all state management
// Primer panel: collapsible accordion at top of right panel
// Tab bar pattern: activeTab state + conditional rendering
```

From .planning/phases/09-st-005-tool-orchestration/09-CONTEXT.md (locked decisions for artifact):
```
Node rendering: tool name + criticality dot (green/amber/red)
Edge styles: solid=required, dashed=optional, dotted=enhances, all teal color
Edge creation: select dependency type first via 3 toolbar buttons, then click source->target
Auto-layout button + manual drag
Fixed viewport with fit-to-canvas scaling
Right panel: Topology / Health / Interventions tabs, ~280px width
Primer panel: collapsible accordion at TOP of right panel
Two modes: Build (add tools, draw edges) + Analyze (blast radius, failure sim, health)
Worked example auto-starts in analyze mode
'Start Fresh' button clears canvas, switches to build mode
'Keep Example + Add' option lets students extend worked example
Tool input: name + criticality dropdown only (advanced fields behind toggle)
Isolated tools: 50% opacity with 'isolated' label
Animated dot pulse along edges on selected tool's connections
```

From .planning/phases/09-st-005-tool-orchestration/09-CONTEXT.md (worked example):
```
9 tools across 2 servers:
- week1-foundations server (3 tools): create_causal_loop, find_leverage_points, analyze_system
- systems-thinking-cubelets server (6 tools): score_reinforcing_loops, compare_interventions, detect_burden_shift, analyze_agent_feedback_loops, analyze_tool_orchestration, detect_automation_debt
- 3 planned tools rendered with dashed border + 'planned' badge
- create_causal_loop pre-failed on first load -- cascade visible immediately
- Subtle background region per server (faint panel) with server name label
- 3 pre-annotated interventions at L1/L6/L10
- Inline annotation callouts (numbered bubbles 1, 2, 3), dismissible
```

Health scoring (from CONTEXT.md):
```
Complexity: edge_count + (cycle_count * penalty_multiplier)
Redundancy: count of tool pairs with matching input AND output overlap
Brittleness: max blast radius depth (longest cascade via required deps only)
Aggregate: equal-weight average (33/33/33)
Higher = unhealthier (0-100 scale)
Tiers: Healthy 0-33 (green), At Risk 34-66 (amber), Critical 67-100 (red)
Display: numbers + horizontal color-coded bars
Live update on graph changes
Per-tool breakdown on selection
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 test infrastructure -- vitest config, test stubs, and fixture</name>
  <files>preview-app/vitest.config.js, preview-app/src/__tests__/tool-orchestration-analyzer.test.jsx, preview-app/src/__tests__/fixtures/worked-example-tool-stack.json</files>
  <action>
    Create the test infrastructure required by VALIDATION.md Wave 0 requirements. This establishes the Nyquist contract -- tests exist BEFORE artifact implementation.

    **Step 1: Install vitest and testing deps (if not already installed):**
    ```bash
    cd preview-app && npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom
    ```

    **Step 2: Create vitest.config.js in preview-app/:**
    ```js
    import { defineConfig } from 'vitest/config'
    import react from '@vitejs/plugin-react'

    export default defineConfig({
      plugins: [react()],
      test: {
        environment: 'jsdom',
        globals: true,
        setupFiles: [],
      },
    })
    ```

    **Step 3: Create worked-example-tool-stack.json fixture:**
    JSON file with the 9-tool course stack and 12 dependencies matching the CONTEXT.md worked example spec. Structure:
    ```json
    {
      "tools": [
        { "tool_id": "create_causal_loop", "name": "Create Causal Loop", "server": "week1-foundations", "criticality": "high", "inputs": ["variables", "causal_links"], "outputs": ["cld_graph"] },
        { "tool_id": "find_leverage_points", "name": "Find Leverage Points", "server": "week1-foundations", "criticality": "medium", "inputs": ["variables", "causal_links"], "outputs": ["leverage_scores"] },
        { "tool_id": "analyze_system", "name": "Analyze System", "server": "week1-foundations", "criticality": "low", "inputs": ["system_description"], "outputs": ["analysis"] },
        { "tool_id": "score_reinforcing_loops", "name": "Score Reinforcing Loops", "server": "systems-thinking-cubelets", "criticality": "high", "inputs": ["variables", "causal_links"], "outputs": ["loops", "severity"] },
        { "tool_id": "compare_interventions", "name": "Compare Interventions", "server": "systems-thinking-cubelets", "criticality": "medium", "inputs": ["interventions", "leverage_scores"], "outputs": ["ranked_comparison"] },
        { "tool_id": "detect_burden_shift", "name": "Detect Burden Shift", "server": "systems-thinking-cubelets", "criticality": "medium", "inputs": ["fix_records"], "outputs": ["burden_analysis"] },
        { "tool_id": "analyze_agent_feedback_loops", "name": "Analyze Agent Feedback Loops", "server": "systems-thinking-cubelets", "criticality": "high", "inputs": ["components", "links"], "outputs": ["loops", "severity"], "planned": true },
        { "tool_id": "analyze_tool_orchestration", "name": "Analyze Tool Orchestration", "server": "systems-thinking-cubelets", "criticality": "high", "inputs": ["tools", "dependencies"], "outputs": ["health_scores"], "planned": true },
        { "tool_id": "detect_automation_debt", "name": "Detect Automation Debt", "server": "systems-thinking-cubelets", "criticality": "medium", "inputs": ["automation_layers"], "outputs": ["debt_analysis"], "planned": true }
      ],
      "dependencies": [
        { "from_tool": "create_causal_loop", "to_tool": "score_reinforcing_loops", "dependency_type": "required", "data_fields": ["variables", "causal_links"] },
        { "from_tool": "create_causal_loop", "to_tool": "find_leverage_points", "dependency_type": "required", "data_fields": ["variables", "causal_links"] },
        { "from_tool": "create_causal_loop", "to_tool": "analyze_agent_feedback_loops", "dependency_type": "required", "data_fields": ["cld_graph"] },
        { "from_tool": "find_leverage_points", "to_tool": "compare_interventions", "dependency_type": "required", "data_fields": ["leverage_scores"] },
        { "from_tool": "score_reinforcing_loops", "to_tool": "compare_interventions", "dependency_type": "optional", "data_fields": ["loops"] },
        { "from_tool": "score_reinforcing_loops", "to_tool": "analyze_agent_feedback_loops", "dependency_type": "enhances", "data_fields": ["scoring_patterns"] },
        { "from_tool": "compare_interventions", "to_tool": "analyze_tool_orchestration", "dependency_type": "enhances", "data_fields": ["intervention_format"] },
        { "from_tool": "detect_burden_shift", "to_tool": "detect_automation_debt", "dependency_type": "required", "data_fields": ["burden_patterns"] },
        { "from_tool": "analyze_agent_feedback_loops", "to_tool": "analyze_tool_orchestration", "dependency_type": "optional", "data_fields": ["feedback_analysis"] },
        { "from_tool": "analyze_system", "to_tool": "find_leverage_points", "dependency_type": "optional", "data_fields": ["system_context"] },
        { "from_tool": "analyze_system", "to_tool": "detect_burden_shift", "dependency_type": "optional", "data_fields": ["system_context"] },
        { "from_tool": "score_reinforcing_loops", "to_tool": "detect_burden_shift", "dependency_type": "enhances", "data_fields": ["loop_data"] }
      ]
    }
    ```

    **Step 4: Create tool-orchestration-analyzer.test.jsx with stub tests that FAIL:**
    All stubs use `expect(true).toBe(false)` or `throw new Error('Wave 0 stub')` pattern so they fail until implementation.

    Stubs required for TO-02 (graph rendering):
    - test_graph_renders_with_tools: stub asserting component renders tool nodes on canvas
    - test_redundancy_detection: stub asserting redundant tool pairs are detected and highlighted
    - test_blast_radius_cascade: stub asserting blast radius BFS computes correct downstream tools

    Stubs required for TO-03 (intervention scoring):
    - test_intervention_meadows_assignment: stub asserting action types map to correct Meadows levels

    Stubs required for TO-04 (worked example):
    - test_worked_example_loads: stub asserting worked example loads with 9 tools and 12 edges
    - test_worked_example_prefailed: stub asserting create_causal_loop starts in failed state

    Stubs required for TO-05 (health scoring):
    - test_health_score_calculation: stub asserting calculateHealthScores returns 4 scores 0-100
    - test_health_score_tiers: stub asserting tier assignment (Healthy/At Risk/Critical)
    - test_health_scores_live_update: stub asserting health scores recalculate on graph changes

    Each stub:
    ```jsx
    test('description matching TO requirement', () => {
      // Wave 0 stub -- implement when artifact is built in Task 1
      throw new Error('Wave 0 stub -- not yet implemented');
    });
    ```

    Import structure (placeholder until artifact exists):
    ```jsx
    import { describe, test, expect } from 'vitest';
    // React/RTL imports will be added when artifact implementation begins
    ```

    Include a module comment: "Wave 0 test stubs for ST-005 Tool Orchestration Analyzer artifact (TO-02, TO-03, TO-04, TO-05). Stubs fail until implementation in Task 1."
  </action>
  <verify>
    <automated>cd "preview-app" && node -e "
const fs = require('fs');
const checks = {};
checks['vitest.config.js exists'] = fs.existsSync('vitest.config.js');
checks['test file exists'] = fs.existsSync('src/__tests__/tool-orchestration-analyzer.test.jsx');
checks['fixture exists'] = fs.existsSync('src/__tests__/fixtures/worked-example-tool-stack.json');

if (checks['fixture exists']) {
  const fixture = JSON.parse(fs.readFileSync('src/__tests__/fixtures/worked-example-tool-stack.json', 'utf8'));
  checks['fixture has 9 tools'] = fixture.tools && fixture.tools.length === 9;
  checks['fixture has 12 deps'] = fixture.dependencies && fixture.dependencies.length === 12;
}

if (checks['test file exists']) {
  const content = fs.readFileSync('src/__tests__/tool-orchestration-analyzer.test.jsx', 'utf8');
  const testCount = (content.match(/test\(/g) || []).length;
  checks['test stubs >= 9'] = testCount >= 9;
  console.log('Test stub count: ' + testCount);
}

let allPass = true;
for (const [name, ok] of Object.entries(checks)) {
  console.log((ok ? 'PASS' : 'FAIL') + ': ' + name);
  if (!ok) allPass = false;
}
console.log(allPass ? 'OK' : 'FAIL');
" 2>&1</automated>
  </verify>
  <done>vitest.config.js created, test file has 9+ stubs covering TO-02/TO-03/TO-04/TO-05, fixture has 9 tools and 12 dependencies. All stubs fail by design. Vitest and testing-library deps installed.</done>
</task>

<task type="auto">
  <name>Task 1: Build core artifact with graph rendering, modes, and worked example</name>
  <files>Interactive Artifact for Cubelets/tool-orchestration-analyzer.jsx</files>
  <action>
    Create tool-orchestration-analyzer.jsx as a self-contained React artifact following ST-004 patterns. Use compressed coding style from Phase 8 Plan 05 (shared style objects S.*, rendering helpers). Target under 550 lines for sandbox compliance.

    **Architecture (3-panel layout):**
    - Left toolbar (~72px): Build/Analyze mode toggle, tool creation button (Build mode), dependency type buttons x3 (Build mode: required/optional/enhances), auto-layout button, Start Fresh / Keep Example + Add buttons
    - Central SVG canvas (flex): renders tool nodes as rounded rectangles with tool name + criticality dot, dependency edges with line style differentiation, annotation callouts in worked example
    - Right panel (~280px): Primer panel (collapsible accordion) + 3 tabs (Topology / Health / Interventions)

    **COLORS constant:** Reuse exact dark cybernetic palette from ST-004: bg=#0f1117, canvas=#161922, accent=#00d4aa (teal), accentDanger=#ff6b6b (coral), accentWarm=#ffb347 (amber), textPrimary=#e8ecf4, textSecondary=#7a839e, textMuted=#4a5270, panel=#181c28, panelBorder=#242940

    **Node rendering:**
    - Rounded rectangles (rx=8) with tool name text centered
    - Criticality dot: 6px circle, green (#00d4aa) for low, amber (#ffb347) for medium, red (#ff6b6b) for high/critical
    - Planned tools: dashed stroke + small "planned" badge text
    - Isolated tools (no edges): 50% opacity with "isolated" label
    - Selected node: accent border glow
    - Failed node: red fill (#ff6b6b at 20% opacity) with red border

    **Edge rendering:**
    - Quadratic Bezier paths (reuse getEdgePath from ST-004 with curve adjustment for reverse edges)
    - Line styles: solid strokeDasharray="" for required, "8 4" for optional, "3 3" for enhances
    - All teal (#00d4aa) color
    - Edge labels: dependency type visible on path (small text at midpoint)
    - Animated dot pulse on selected tool's connections (subtle, using CSS animation keyframes in style tag)
    - Arrow markers at edge endpoints

    **Edge creation (Build mode):**
    - 3 toolbar buttons for dependency type selection (required/optional/enhances)
    - Click source node -> click target node to create edge of selected type
    - Visual feedback: source node highlighted while waiting for target click

    **Tool creation (Build mode):**
    - Click "Add Tool" button -> inline form appears: name input + criticality dropdown (low/medium/high)
    - "Advanced" toggle reveals: server, inputs (comma-separated), outputs (comma-separated), latency, description
    - Place on canvas at next available position

    **Topological sort layout:**
    - Kahn's algorithm: compute in-degree, process zero-in-degree nodes in layers
    - Place layers top-to-bottom with increasing y, nodes in each layer spread horizontally
    - Handle cycles gracefully (remaining nodes placed at bottom)
    - Auto-layout button triggers recalculation
    - Manual drag adjustment (mousedown/mousemove/mouseup on nodes)

    **Worked example (WORKED_EXAMPLE constant):**
    Define 9 tools with positions, servers, criticalities, inputs/outputs:
    1. create_causal_loop (week1-foundations, high criticality, inputs: variables/causal_links, outputs: cld_graph)
    2. find_leverage_points (week1-foundations, medium, inputs: variables/causal_links, outputs: leverage_scores)
    3. analyze_system (week1-foundations, low, inputs: system_description, outputs: analysis)
    4. score_reinforcing_loops (systems-thinking-cubelets, high, inputs: variables/causal_links, outputs: loops/severity)
    5. compare_interventions (systems-thinking-cubelets, medium, inputs: interventions/leverage_scores, outputs: ranked_comparison)
    6. detect_burden_shift (systems-thinking-cubelets, medium, inputs: fix_records, outputs: burden_analysis)
    7. analyze_agent_feedback_loops (systems-thinking-cubelets, high, inputs: components/links, outputs: loops/severity) -- planned, dashed border
    8. analyze_tool_orchestration (systems-thinking-cubelets, high, inputs: tools/dependencies, outputs: health_scores) -- planned, dashed border
    9. detect_automation_debt (systems-thinking-cubelets, medium, inputs: automation_layers, outputs: debt_analysis) -- planned, dashed border

    Dependencies (12 edges):
    - create_causal_loop -> score_reinforcing_loops (required, data: variables/causal_links)
    - create_causal_loop -> find_leverage_points (required, data: variables/causal_links)
    - create_causal_loop -> analyze_agent_feedback_loops (required, data: cld_graph)
    - find_leverage_points -> compare_interventions (required, data: leverage_scores)
    - score_reinforcing_loops -> compare_interventions (optional, data: loops)
    - score_reinforcing_loops -> analyze_agent_feedback_loops (enhances, data: scoring_patterns)
    - compare_interventions -> analyze_tool_orchestration (enhances, data: intervention_format)
    - detect_burden_shift -> detect_automation_debt (required, data: burden_patterns)
    - analyze_agent_feedback_loops -> analyze_tool_orchestration (optional, data: feedback_analysis)
    - analyze_system -> find_leverage_points (optional, data: system_context)
    - analyze_system -> detect_burden_shift (optional, data: system_context)
    - score_reinforcing_loops -> detect_burden_shift (enhances, data: loop_data)

    Pre-failed state: create_causal_loop starts with failed=true, cascade computed on mount.

    Subtle background regions per server: faint lighter panel (#1e2230 at 40% opacity) with server name label.

    3 pre-annotated interventions (shown in Interventions tab):
    - L1: "Increase timeout on create_causal_loop" (parameter tweak)
    - L6: "Add caching layer between CLD creation and consumers" (feedback structure)
    - L10: "Redesign so each tool is self-contained" (system goals)

    Inline annotation callouts: 3 numbered bubbles on canvas pointing to (1) pre-failed tool, (2) cascade highlight, (3) high-brittleness path. Dismissible with X button. Stored in showAnnotations state.

    Auto-starts in Analyze mode.

    **Health scoring (calculateHealthScores function):**
    - Complexity: min(100, edges.length + cycles.length * 10)
    - Redundancy: count tool pairs where intersection of inputs AND intersection of outputs are both non-empty; min(100, count * 20)
    - Brittleness: BFS from each tool following only "required" edges, track max depth; min(100, maxDepth * 20)
    - Aggregate: Math.round((complexity + redundancy + brittleness) / 3)
    - Tier: score <= 33 ? "healthy" : score <= 66 ? "at-risk" : "critical"
    - Live recalculation on any graph change (in useEffect with [nodes, edges] deps)

    **Right panel:**
    - Primer panel (collapsible accordion at top): Brief intro to tool orchestration, dependency types (required/optional/enhances), health metrics, Meadows hierarchy reminder. Cross-references ST-002 concepts. Collapsed by default.
    - Topology tab: tool count, edge count, cycle count, isolated tool count, server breakdown
    - Health tab: 4 horizontal bars (complexity, redundancy, brittleness, aggregate) with score number + tier label + color (green/amber/red). Per-tool breakdown when a tool is selected (click tool -> shows its individual contribution to each metric)
    - Interventions tab: Pre-annotated interventions for worked example (L1/L6/L10 with descriptions). In Plan 04 this tab will gain progressive disclosure prediction UX.

    **State management (useState):**
    - nodes, edges (graph data)
    - mode: "build" | "analyze"
    - selectedTool (null or tool id)
    - activeTab: "topology" | "health" | "interventions"
    - primerCollapsed (true by default)
    - healthScores (computed object)
    - detectedCycles (array)
    - showAnnotations (boolean, true in worked example)
    - edgeType: "required" | "optional" | "enhances" (for Build mode edge creation)
    - edgeSource (null or tool id, for edge creation flow)
    - showAddForm (boolean for tool creation form)
    - failedTools (Set of tool ids)
    - isExample (boolean -- true when worked example loaded)

    **Compression strategy (from Phase 8 Plan 05):**
    - Shared style objects: S.flexCol, S.flexRow, S.labelSm, S.cardBase, S.tabBtn, S.toolbarBtn, S.divider, S.selectInput, S.btnLabel
    - Rendering helpers: renderNode, renderEdge, renderHealthBar, renderAnnotation
    - Compact JSX patterns, single-line styles for <=3 properties
    - Target: under 550 lines total
  </action>
  <verify>
    <automated>python -c "
import os
path = 'Interactive Artifact for Cubelets/tool-orchestration-analyzer.jsx'
if not os.path.exists(path):
    print('FAIL: File not found'); exit(1)
content = open(path, encoding='utf-8').read()
lines = len(content.split('\n'))
checks = {
    'WORKED_EXAMPLE or 9 tools': 'create_causal_loop' in content,
    'calculateHealthScores': 'calculateHealthScores' in content or 'HealthScore' in content or 'complexity' in content,
    'Topology tab': 'topology' in content.lower(),
    'Health tab': 'health' in content.lower(),
    'Interventions tab': 'intervention' in content.lower(),
    'Build/Analyze modes': 'build' in content.lower() and 'analyze' in content.lower(),
    'Primer/ST-002 ref': 'ST-002' in content or 'Meadows' in content or 'leverage' in content.lower(),
    'Teal color': '#00d4aa' in content,
    'Under 600 lines': lines <= 600,
}
for name, ok in checks.items():
    print(f'  {\"PASS\" if ok else \"FAIL\"}: {name}')
print(f'Lines: {lines}')
all_pass = all(checks.values())
print(f'Overall: {\"OK\" if all_pass else \"FAIL\"}')" 2>&1</automated>
  </verify>
  <done>tool-orchestration-analyzer.jsx renders a complete dependency graph artifact with worked example (9 tools, create_causal_loop pre-failed), Build/Analyze modes, health scoring, tabbed right panel (Topology/Health/Interventions), primer panel with ST-002 references, under 550 lines</done>
</task>

<task type="auto">
  <name>Task 2: Verify artifact renders and worked example cascade is correct</name>
  <files>Interactive Artifact for Cubelets/tool-orchestration-analyzer.jsx</files>
  <action>
    Run structural verification on the artifact:
    1. Verify JSX syntax is valid (balanced braces, proper React component export)
    2. Verify worked example has exactly 9 tools and 12 dependency edges
    3. Verify health scores calculate correctly for the worked example:
       - Complexity should be non-zero (12 edges + cycle penalty if cycles exist)
       - Brittleness should be non-zero (create_causal_loop has 3+ required downstream)
       - All scores are 0-100
    4. Verify all 3 dependency types are represented in edges (required, optional, enhances)
    5. Verify create_causal_loop is in the initial failed set
    6. Verify line count is within sandbox limit (under 550 lines, under 55KB)
    7. Verify dark cybernetic palette colors are present

    If any issues found, fix them in the artifact file. Common issues from ST-004 experience:
    - Stale closure in useEffect: use ref pattern (prevRef.current) instead of state in deps
    - alert() calls: replace with inline warning banners
    - Exceeding line limit: apply more aggressive compression (shared styles, helper extraction)
  </action>
  <verify>
    <automated>python -c "
import os, re
path = 'Interactive Artifact for Cubelets/tool-orchestration-analyzer.jsx'
content = open(path, encoding='utf-8').read()
lines = len(content.split('\n'))
size = os.path.getsize(path)

# Count tools in worked example
tool_count = content.count('tool_id') if 'tool_id' in content else content.count('\"id\"')
has_9_tools = 'create_causal_loop' in content and 'detect_automation_debt' in content

# Check dependency types
has_required = 'required' in content
has_optional = 'optional' in content
has_enhances = 'enhances' in content

# Check for alert()
has_alert = 'alert(' in content

# Check for React export
has_export = 'export default' in content or 'function ' in content

print(f'Lines: {lines} (limit 550)')
print(f'Size: {size} bytes (limit 55000)')
print(f'Has 9 tools: {has_9_tools}')
print(f'Dep types (req/opt/enh): {has_required}/{has_optional}/{has_enhances}')
print(f'No alert(): {not has_alert}')
print(f'React component: {has_export}')
ok = lines <= 550 and size <= 55000 and has_9_tools and has_required and has_optional and has_enhances and not has_alert and has_export
print(f'Overall: {\"OK\" if ok else \"FAIL\"}')" 2>&1</automated>
  </verify>
  <done>Artifact is structurally valid, worked example has 9 tools with all 3 dependency types, under sandbox limits (550 lines, 55KB), no alert() calls, ready for Plan 04 enhancements</done>
</task>

</tasks>

<verification>
- vitest.config.js exists in preview-app/
- Test stubs exist with 9+ tests covering TO-02, TO-03, TO-04, TO-05
- Fixture has 9 tools and 12 dependencies matching worked example
- tool-orchestration-analyzer.jsx exists and is under 550 lines / 55KB
- Worked example loads with 9 tools across 2 servers
- create_causal_loop pre-failed with cascade visible
- Build/Analyze mode toggle works
- Health scores display in right panel
- Primer panel references ST-002 concepts
- All 3 dependency types rendered with correct line styles
- No alert() calls, no external dependencies
</verification>

<success_criteria>
- Wave 0 test infrastructure in place (vitest config, stubs, fixture)
- Complete React artifact with graph visualization, worked example, and health scoring
- Dark cybernetic aesthetic matches existing artifacts
- Under sandbox size limits (550 lines, 55KB)
- Tabbed right panel with Topology/Health/Interventions
- Primer panel with ST-002 cross-references
</success_criteria>

<output>
After completion, create `.planning/phases/09-st-005-tool-orchestration/09-03-SUMMARY.md`
</output>
