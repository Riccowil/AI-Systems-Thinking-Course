---
phase: 09-st-005-tool-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cubelets MCP Tool/files/cubelets_mcp_server.py
  - Cubelets MCP Tool/files/test_tool_orchestration.py
autonomous: true
requirements: [TO-06, TO-07]

must_haves:
  truths:
    - "MCP tool analyze_tool_orchestration accepts MCPTool list + ToolDependency list and returns health scores"
    - "MCP tool composes with existing compare_interventions by transforming tool interventions to Intervention format"
    - "Health scores include complexity, redundancy, brittleness sub-scores and aggregate (all 0-100)"
    - "Tool returns textual refactor recommendations for unhealthy metrics"
  artifacts:
    - path: "Cubelets MCP Tool/files/cubelets_mcp_server.py"
      provides: "MCPTool model, ToolDependency model, AnalyzeToolOrchestrationInput model, analyze_tool_orchestration tool"
      contains: "class MCPTool"
    - path: "Cubelets MCP Tool/files/test_tool_orchestration.py"
      provides: "Test suite for tool orchestration models and tool"
      min_lines: 50
  key_links:
    - from: "analyze_tool_orchestration"
      to: "compare_interventions"
      via: "transforms MCPTool interventions to Intervention objects with Meadows levels"
      pattern: "Intervention\\(.*meadows_level"
    - from: "MCPTool"
      to: "ToolDependency"
      via: "cross-reference validation (from_tool/to_tool match tool_ids)"
      pattern: "from_tool.*to_tool"
---

<objective>
Add MCPTool and ToolDependency Pydantic models plus the analyze_tool_orchestration MCP tool to the existing cubelets_mcp_server.py. The tool accepts a list of tools and dependencies, calculates health scores (complexity, redundancy, brittleness), detects cycles, and composes with existing compare_interventions for Meadows-ranked refactor recommendations.

Purpose: Provide the programmatic analysis backend that both the interactive artifact (Plan 03/04) and Claude skill (Plan 02) will reference. Follows the same pattern as Phase 8 Plan 01 (models + tool first).
Output: Extended MCP server with 5th tool (analyze_tool_orchestration) + test suite
</objective>

<execution_context>
@C:/Users/ricco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ricco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-st-005-tool-orchestration/09-CONTEXT.md
@.planning/phases/09-st-005-tool-orchestration/09-RESEARCH.md
@.planning/phases/07-content-foundations/data-model-specs.md

<interfaces>
<!-- Key types and contracts from existing codebase. Executor uses directly. -->

From Cubelets MCP Tool/files/cubelets_mcp_server.py:
```python
# Existing models to reuse
class Polarity(str, Enum):
    POSITIVE = "+"
    NEGATIVE = "-"

class ResponseFormat(str, Enum):
    MARKDOWN = "markdown"
    JSON = "json"

class MeadowsLevel(str, Enum):
    L1 = "1"   # Constants/parameters
    L2 = "2"   # Buffer/stock sizes
    # ... through L12

class Intervention(BaseModel):
    model_config = ConfigDict(str_strip_whitespace=True)
    target_variable: str = Field(...)
    label: str = Field(...)
    description: str = Field(default="")
    meadows_level: MeadowsLevel = Field(...)
    estimated_impact: Optional[str] = Field(default=None)

class CompareInterventionsInput(BaseModel):
    system_description: str
    interventions: List[Intervention]
    leverage_scores: Optional[Dict[str, float]] = None
    response_format: ResponseFormat = ResponseFormat.MARKDOWN

# Existing tool signature
async def compare_interventions(params: CompareInterventionsInput) -> str
```

From .planning/phases/07-content-foundations/data-model-specs.md:
```
MCPTool fields: tool_id (str), name (str), server (str), description (str opt), inputs (List[str] opt), outputs (List[str] opt), estimated_latency_ms (int opt), criticality (Literal["low","medium","high","critical"] opt)
ToolDependency fields: from_tool (str), to_tool (str), dependency_type (Literal["required","optional","enhances"]), data_fields (List[str] opt), description (str opt)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 test stubs for TO-06, TO-07</name>
  <files>Cubelets MCP Tool/files/test_tool_orchestration.py</files>
  <action>
    Create test_tool_orchestration.py with stub test functions that FAIL (raise NotImplementedError or use pytest.fail). This establishes the Nyquist contract -- tests exist BEFORE implementation. Follow the pattern from test_agent_models.py (same directory).

    Stubs required for TO-06 (health scoring):
    - test_mcptool_valid_creation: stub asserting MCPTool can be created with valid fields
    - test_mcptool_rejects_empty_tool_id: stub asserting validation error on empty tool_id
    - test_mcptool_criticality_validation: stub asserting criticality must be low/medium/high/critical
    - test_tool_dependency_valid_creation: stub asserting ToolDependency can be created
    - test_tool_dependency_no_self_reference: stub asserting from_tool != to_tool
    - test_basic_health_scoring: stub asserting analyze_tool_orchestration returns health scores with complexity/redundancy/brittleness/aggregate
    - test_empty_dependencies_zero_scores: stub asserting empty dependency list returns all-zero scores
    - test_cycle_detection: stub asserting cycles are detected in A->B->C->A pattern
    - test_cross_reference_validation: stub asserting from_tool/to_tool reference valid tool_ids

    Stubs required for TO-07 (composition):
    - test_intervention_composition: stub asserting tool composes with compare_interventions via Intervention objects
    - test_meadows_level_mapping: stub asserting action types map to correct Meadows levels (Add=L1, Remove=L2, Cache=L5, Refactor=L6)

    Each stub should have a docstring describing the expected behavior and a body of `pytest.fail("Wave 0 stub -- implement in Task 1/Task 2")`.

    Import structure (will fail until models exist, which is expected):
    ```python
    import pytest
    # Imports will be added in Task 1 when models are created
    ```

    Include a module docstring: "Wave 0 test stubs for ST-005 Tool Orchestration (TO-06, TO-07). Stubs fail until implementation in Tasks 1-2."
  </action>
  <verify>
    <automated>cd "Cubelets MCP Tool/files" && python -c "
import ast, sys
with open('test_tool_orchestration.py') as f:
    tree = ast.parse(f.read())
funcs = [n.name for n in ast.walk(tree) if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef)) and n.name.startswith('test_')]
required = ['test_mcptool_valid_creation', 'test_basic_health_scoring', 'test_intervention_composition', 'test_cycle_detection', 'test_empty_dependencies_zero_scores']
missing = [r for r in required if r not in funcs]
print(f'Test stubs found: {len(funcs)}')
print(f'Required stubs present: {len(required) - len(missing)}/{len(required)}')
if missing: print(f'Missing: {missing}')
print('OK' if not missing and len(funcs) >= 11 else 'FAIL')
" 2>&1</automated>
  </verify>
  <done>test_tool_orchestration.py exists with 11+ test stubs covering TO-06 and TO-07. All stubs fail by design (pytest.fail). File is parseable Python.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add MCPTool and ToolDependency Pydantic models with test suite</name>
  <files>Cubelets MCP Tool/files/cubelets_mcp_server.py, Cubelets MCP Tool/files/test_tool_orchestration.py</files>
  <behavior>
    - Test: MCPTool validates tool_id, name, server are non-empty strings
    - Test: MCPTool accepts optional fields (description, inputs, outputs, criticality, estimated_latency_ms)
    - Test: MCPTool rejects empty tool_id, empty name, empty server
    - Test: MCPTool.criticality must be one of low/medium/high/critical
    - Test: ToolDependency validates from_tool != to_tool (no self-dependencies)
    - Test: ToolDependency.dependency_type must be one of required/optional/enhances
    - Test: ToolDependency with data_fields stores list of strings
  </behavior>
  <action>
    Replace the Wave 0 stubs with real test implementations in test_tool_orchestration.py for the model validation tests. Then add both Pydantic models to cubelets_mcp_server.py in a new "ST-005: Tool Orchestration" section after the ST-004 section.

    MCPTool model spec (from data-model-specs.md):
    - tool_id: str, min_length=1, max_length=100
    - name: str, min_length=1, max_length=200
    - server: str, min_length=1, max_length=100
    - description: str, optional, max_length=500
    - inputs: List[str], default_factory=list
    - outputs: List[str], default_factory=list
    - estimated_latency_ms: Optional[int], ge=0
    - criticality: Optional[Literal["low", "medium", "high", "critical"]]
    - model_config = ConfigDict(str_strip_whitespace=True)

    ToolDependency model spec:
    - from_tool: str, min_length=1, max_length=100
    - to_tool: str, min_length=1, max_length=100
    - dependency_type: Literal["required", "optional", "enhances"]
    - data_fields: List[str], default_factory=list
    - description: str, optional, max_length=300
    - model_config = ConfigDict(str_strip_whitespace=True)
    - Add @model_validator(mode="after") to enforce from_tool != to_tool

    Follow existing patterns: ConfigDict(str_strip_whitespace=True), Field with description.
  </action>
  <verify>
    <automated>cd "Cubelets MCP Tool/files" && python -m pytest test_tool_orchestration.py -x -v 2>&1 | head -50</automated>
  </verify>
  <done>MCPTool and ToolDependency models importable from cubelets_mcp_server.py, all validation tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add analyze_tool_orchestration MCP tool with health scoring and composition</name>
  <files>Cubelets MCP Tool/files/cubelets_mcp_server.py, Cubelets MCP Tool/files/test_tool_orchestration.py</files>
  <behavior>
    - Test: Tool accepts list of MCPTool + ToolDependency and returns health scores
    - Test: Complexity score = edge_count + (cycle_count * 10), clamped 0-100
    - Test: Redundancy score = count of tool pairs with matching inputs AND outputs overlap, scaled to 0-100
    - Test: Brittleness score = max blast radius depth via required-only edges, scaled to 0-100
    - Test: Aggregate = (complexity + redundancy + brittleness) / 3
    - Test: Cycle detection finds A->B->C->A cycle in tool dependencies
    - Test: Tool returns refactor recommendations for scores >= 67 (Critical tier)
    - Test: Tool composes with compare_interventions by creating Intervention objects from tool analysis
    - Test: Empty dependency list returns all-zero health scores
    - Test: Cross-reference validation: from_tool/to_tool must reference valid tool_ids
  </behavior>
  <action>
    Add AnalyzeToolOrchestrationInput model and analyze_tool_orchestration tool function to cubelets_mcp_server.py.

    AnalyzeToolOrchestrationInput:
    - tools: List[MCPTool], min_length=1, max_length=50
    - dependencies: List[ToolDependency], default_factory=list
    - interventions: Optional[List] for optional Meadows scoring (action_type + target_tool pairs)
    - response_format: ResponseFormat = ResponseFormat.MARKDOWN

    Tool implementation:
    1. Cross-validate: all from_tool/to_tool in dependencies must reference valid tool_ids
    2. Build adjacency list from dependencies
    3. Detect cycles using existing DFS pattern (reuse _find_all_cycles helper or implement equivalent)
    4. Calculate health scores:
       - Complexity: min(100, edge_count + cycle_count * 10) -- cycle penalty multiplier = 10 per research recommendation
       - Redundancy: count tool pairs where set(inputs) intersection AND set(outputs) intersection are both non-empty, scale: min(100, count * 20)
       - Brittleness: BFS/DFS from each tool following only "required" edges, max depth found, scale: min(100, max_depth * 20)
       - Aggregate: round((complexity + redundancy + brittleness) / 3)
    5. Tier assignment: 0-33 Healthy, 34-66 At Risk, 67-100 Critical
    6. Generate refactor recommendations for Critical-tier metrics
    7. If interventions provided, compose with compare_interventions:
       - Map action types to Meadows levels: Add tool = L1, Remove tool = L2, Refactor dependency = L6, Add cache/fallback = L5
       - Create Intervention objects with mapped levels
       - Call compare_interventions internally and include results

    Use @mcp.tool decorator with annotations matching existing tools (readOnlyHint=True, idempotentHint=True).

    Replace remaining Wave 0 stubs with real test implementations in test_tool_orchestration.py.
  </action>
  <verify>
    <automated>cd "Cubelets MCP Tool/files" && python -m pytest test_tool_orchestration.py -x -v 2>&1 | head -80</automated>
  </verify>
  <done>analyze_tool_orchestration tool responds with correct health scores, composes with compare_interventions, all tests pass. Server now has 5 MCP tools.</done>
</task>

</tasks>

<verification>
- All tests in test_tool_orchestration.py pass
- cubelets_mcp_server.py imports without errors
- Server tool count is now 5 (ST-001, ST-002, ST-003, ST-004, ST-005)
- Health scores are 0-100 for all sub-scores and aggregate
- Cycle detection finds circular dependencies
- Composition with compare_interventions produces ranked intervention analysis
</verification>

<success_criteria>
- MCPTool and ToolDependency models match Phase 7 data-model-specs.md
- analyze_tool_orchestration returns health_scores with complexity/redundancy/brittleness/aggregate
- Tool composes with compare_interventions via Intervention object transformation
- Full test suite passes (model validation + tool behavior)
</success_criteria>

<output>
After completion, create `.planning/phases/09-st-005-tool-orchestration/09-01-SUMMARY.md`
</output>
