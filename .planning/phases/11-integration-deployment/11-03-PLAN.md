---
phase: 11-integration-deployment
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified: []
autonomous: false
requirements: [INTG-02, INTG-03, INTG-06, INTG-07]

must_haves:
  truths:
    - "All 6 MCP tools respond correctly when called"
    - "All 3 new Claude skills are installed and functional"
    - "Dark cybernetic aesthetic is consistent across all 9 artifact tabs"
    - "Vercel deployment is live with all 9 artifacts functional"
  artifacts: []
  key_links:
    - from: "Cubelets MCP Tool/files/cubelets_mcp_server.py"
      to: "6 registered tools"
      via: "@mcp.tool decorator"
      pattern: "@mcp.tool"
    - from: "preview-app (deployed)"
      to: "Vercel production URL"
      via: "git push to main triggers auto-deploy"
      pattern: "vercel"
---

<objective>
Verify MCP server, Claude skills, theme consistency, and deploy to Vercel.

Purpose: Confirm all integration points work end-to-end and ship to production.
Output: Live Vercel deployment with all 9 artifacts, verified MCP server and skills.
</objective>

<execution_context>
@C:/Users/ricco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ricco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-integration-deployment/11-01-SUMMARY.md
@.planning/phases/11-integration-deployment/11-02-SUMMARY.md

<interfaces>
<!-- MCP server has 6 tools (from cubelets_mcp_server.py): -->
1. score_reinforcing_loops (ST-001)
2. compare_interventions (ST-002)
3. detect_burden_shift (ST-003)
4. analyze_agent_feedback_loops (ST-004)
5. analyze_tool_orchestration (ST-005)
6. detect_automation_debt (ST-006)

<!-- Claude skills (from Claude skills build for Cubelets/files/): -->
- agent-feedback-analyzer.skill (ST-004)
- tool-stack-analyzer.skill (ST-005)
- automation-debt-detector.skill (ST-006)

<!-- Vercel config: -->
- projectId: prj_APAKi6GOWFmnAVJ0GaVAMJmxBdIn
- Push to main auto-deploys
- URL: https://preview-app-two.vercel.app
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify MCP server exposes 6 tools</name>
  <files>Cubelets MCP Tool/files/cubelets_mcp_server.py</files>
  <action>
1. Run the existing MCP server test files to confirm all 6 tools work:
   - `python3 "Cubelets MCP Tool/files/test_agent_models.py"` (tests ST-004 tool)
   - `python3 "Cubelets MCP Tool/files/test_tool_orchestration.py"` (tests ST-005 tool)
2. If tests pass, confirm all 6 @mcp.tool decorators exist in cubelets_mcp_server.py by grepping
3. If any test fails, diagnose and report the failure (do NOT modify the server -- it was verified in prior phases)

Note: INTG-02 requires the server to expose 6 tools. The server already has all 6 from Phases 8-10. This task verifies rather than builds.
  </action>
  <verify>
    <automated>cd "Cubelets MCP Tool/files" && python3 test_agent_models.py 2>&1 | tail -3 && python3 test_tool_orchestration.py 2>&1 | tail -3</automated>
  </verify>
  <done>All 6 MCP tools verified functional: score_reinforcing_loops, compare_interventions, detect_burden_shift, analyze_agent_feedback_loops, analyze_tool_orchestration, detect_automation_debt</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Claude skills, theme consistency, and Vercel deployment</name>
  <files></files>
  <action>
CHECKPOINT: Human verification required for visual and functional checks that Claude cannot automate.

What was built: Complete integration of 9 cubelets -- preview app with week-grouped tabs, MCP server with 6 tools, course infrastructure updated.
  </action>
  <verify>
Human performs the following checks:

**Claude Skills (INTG-03):**
1. Confirm these 3 skill files exist in `Claude skills build for Cubelets/files/`:
   - agent-feedback-analyzer.skill
   - tool-stack-analyzer.skill
   - automation-debt-detector.skill
2. Install each skill in Claude Desktop if not already installed
3. Invoke each skill by name and confirm the multi-step workflow runs

**Theme Consistency (INTG-06):**
4. Run `cd preview-app && npm run dev`
5. Open http://localhost:5173 in browser
6. Click through all 9 tabs one by one
7. Confirm dark cybernetic palette is consistent -- no jarring color breaks, typography mismatches, or component style differences between tabs
8. If any tab looks off, note which one and what looks wrong

**Vercel Deployment (INTG-07):**
9. Merge changes to main branch (or push if already on main)
10. Wait for Vercel auto-deploy (usually ~1 minute)
11. Visit https://preview-app-two.vercel.app
12. Confirm all 9 tabs load and render correctly at the production URL
  </verify>
  <done>User types "approved" confirming: 3 skills functional, 9 tabs visually consistent, Vercel deployment live at production URL</done>
</task>

</tasks>

<verification>
- MCP server tests pass for all tools
- All 3 new skills exist and are installable
- All 9 tabs render with consistent dark theme
- Production URL serves all 9 artifacts
</verification>

<success_criteria>
All integration verified: 6 MCP tools functional, 3 skills installable, 9 artifacts visually consistent, Vercel deployment live.
</success_criteria>

<output>
After completion, create `.planning/phases/11-integration-deployment/11-03-SUMMARY.md`
</output>
