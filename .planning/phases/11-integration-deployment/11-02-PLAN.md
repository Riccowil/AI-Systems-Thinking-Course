---
phase: 11-integration-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - master-syllabus.json
  - prerequisite-chain.json
autonomous: true
requirements: [INTG-04, INTG-05]

must_haves:
  truths:
    - "master-syllabus.json Module M3 has correct lesson structure for ST-004/005/006"
    - "prerequisite-chain.json has ST-004/005/006 with correct dependencies"
    - "ST-004 requires ST-001, ST-005 requires ST-002 + ST-004, ST-006 requires ST-003"
    - "Artifact filenames in syllabus match actual filenames in preview-app/src/"
  artifacts:
    - path: "master-syllabus.json"
      provides: "Week 3 syllabus with 3 lessons"
      contains: "ST-004"
    - path: "prerequisite-chain.json"
      provides: "Full 9-cubelet prerequisite chain"
      contains: "ST-006"
  key_links:
    - from: "master-syllabus.json"
      to: "prerequisite-chain.json"
      via: "cubelet_id references must match"
      pattern: "ST-00[456]"
---

<objective>
Verify and correct course infrastructure files (syllabus and prerequisite chain) for the 3 new cubelets.

Purpose: Course metadata accurately describes Week 3 content so students and tooling reference correct information.
Output: Verified and corrected master-syllabus.json and prerequisite-chain.json.
</objective>

<execution_context>
@C:/Users/ricco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ricco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Prerequisite ordering per CFND-01 (user decision): -->
- ST-004 requires ST-001
- ST-005 requires ST-002 + ST-004
- ST-006 requires ST-003

<!-- Actual artifact filenames (from Interactive Artifact for Cubelets/): -->
- agent-feedback-loop-builder.jsx (ST-004)
- tool-orchestration-analyzer.jsx (ST-005)
- automation-debt-simulator.jsx (ST-006)

<!-- Current syllabus references agent-feedback-mapper.jsx for ST-004 -- this is WRONG, must be corrected -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix master-syllabus.json Module M3</name>
  <files>master-syllabus.json</files>
  <action>
1. Read master-syllabus.json fully
2. Verify Module M3 (Week 3) has 3 lessons: ST-004, ST-005, ST-006
3. Fix artifact filename references to match actual files:
   - ST-004 artifact file should be `agent-feedback-loop-builder.jsx` (currently listed as `agent-feedback-mapper.jsx` -- FIX THIS)
   - ST-005 artifact file should be `tool-orchestration-analyzer.jsx`
   - ST-006 artifact file should be `automation-debt-simulator.jsx`
4. Verify learning objectives, assessment criteria, and deliverable descriptions are reasonable (these were created in Phase 7)
5. Verify prerequisites array in each lesson:
   - 3.1 (ST-004): prerequisites should be ["ST-001"]
   - 3.2 (ST-005): prerequisites should be ["ST-002", "ST-004"]
   - 3.3 (ST-006): prerequisites should be ["ST-003"]
6. Ensure `total_cubelets: 9` and `total_deliverables: 27` in course header
7. Write corrected file
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('master-syllabus.json')); m3=d['modules'][2]; assert len(m3['lessons'])==3; assert m3['lessons'][0]['deliverables']['artifact']['file']=='agent-feedback-loop-builder.jsx'; print('PASS: M3 verified')"</automated>
  </verify>
  <done>master-syllabus.json M3 has 3 lessons with correct artifact filenames and prerequisites</done>
</task>

<task type="auto">
  <name>Task 2: Verify prerequisite-chain.json ST-004/005/006 entries</name>
  <files>prerequisite-chain.json</files>
  <action>
1. Read prerequisite-chain.json fully
2. Verify ST-004 entry:
   - prerequisites: ["ST-001"]
   - enables: ["ST-005"]
3. Verify ST-005 entry:
   - prerequisites: ["ST-002", "ST-004"]
   - enables: [] (no downstream dependents)
4. Verify ST-006 entry:
   - prerequisites: ["ST-003"]
   - enables: [] (no downstream dependents)
5. Verify the `enables` arrays of existing cubelets include new downstream references:
   - ST-001 enables should include "ST-004"
   - ST-002 enables should include "ST-005"
   - ST-003 enables should include "ST-006"
6. Verify chain order array is ["W1-C1", "W1-C2", "W1-C3", "ST-001", "ST-002", "ST-003", "ST-004", "ST-005", "ST-006"]
7. Fix any discrepancies and write corrected file
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('prerequisite-chain.json')); chain=d['prerequisite_chain']['chain']; st4=[c for c in chain if c['cubelet_id']=='ST-004'][0]; assert st4['prerequisites']==['ST-001']; st5=[c for c in chain if c['cubelet_id']=='ST-005'][0]; assert 'ST-002' in st5['prerequisites'] and 'ST-004' in st5['prerequisites']; st6=[c for c in chain if c['cubelet_id']=='ST-006'][0]; assert st6['prerequisites']==['ST-003']; print('PASS: Prerequisites verified')"</automated>
  </verify>
  <done>prerequisite-chain.json has correct dependency ordering for all 9 cubelets</done>
</task>

</tasks>

<verification>
- master-syllabus.json M3 module has 3 lessons with correct filenames
- prerequisite-chain.json has correct dependency graph for ST-004/005/006
- No circular dependencies exist
- Artifact filenames match actual files on disk
</verification>

<success_criteria>
Course infrastructure files accurately describe all 9 cubelets with correct filenames, prerequisites, and metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/11-integration-deployment/11-02-SUMMARY.md`
</output>
