---
phase: 11-integration-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - preview-app/src/agent-feedback-loop-builder.jsx
  - preview-app/src/tool-orchestration-analyzer.jsx
  - preview-app/src/automation-debt-simulator.jsx
  - preview-app/src/App.jsx
autonomous: true
requirements: [INTG-01, INTG-08]

must_haves:
  truths:
    - "Preview app renders 9 tabs organized by week with visual separators"
    - "Each of ST-004, ST-005, ST-006 tabs lazy-loads its artifact"
    - "Tab labels follow ST-XXX prefix pattern matching existing tabs"
    - "App builds without errors (no dagre dependency needed)"
  artifacts:
    - path: "preview-app/src/agent-feedback-loop-builder.jsx"
      provides: "ST-004 artifact in preview app"
    - path: "preview-app/src/tool-orchestration-analyzer.jsx"
      provides: "ST-005 artifact in preview app"
    - path: "preview-app/src/automation-debt-simulator.jsx"
      provides: "ST-006 artifact in preview app"
    - path: "preview-app/src/App.jsx"
      provides: "9-tab layout with week grouping"
      contains: "agent-feedback-loop-builder"
  key_links:
    - from: "preview-app/src/App.jsx"
      to: "preview-app/src/agent-feedback-loop-builder.jsx"
      via: "lazy(() => import('./agent-feedback-loop-builder.jsx'))"
      pattern: "lazy.*agent-feedback"
    - from: "preview-app/src/App.jsx"
      to: "preview-app/src/tool-orchestration-analyzer.jsx"
      via: "lazy(() => import('./tool-orchestration-analyzer.jsx'))"
      pattern: "lazy.*tool-orchestration"
    - from: "preview-app/src/App.jsx"
      to: "preview-app/src/automation-debt-simulator.jsx"
      via: "lazy(() => import('./automation-debt-simulator.jsx'))"
      pattern: "lazy.*automation-debt"
---

<objective>
Wire all 3 new cubelets (ST-004, ST-005, ST-006) into the preview app with week-grouped tab navigation.

Purpose: Students access all 9 cubelets through a single app with organized tab navigation.
Output: Preview app with 9 lazy-loaded tabs grouped by Week 1 / Week 2 / Week 3.
</objective>

<execution_context>
@C:/Users/ricco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ricco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current App.jsx structure (will be modified) -->
From preview-app/src/App.jsx:
```jsx
const artifacts = [
  { id: 'w1c1', label: 'W1-C1: Systems Thinking', component: lazy(() => import('./systems-thinking-fundamentals.jsx')) },
  { id: 'w1c2', label: 'W1-C2: AI System Lever', component: lazy(() => import('./ai-system-lever.jsx')) },
  { id: 'w1c3', label: 'W1-C3: SMB Mindset Shift', component: lazy(() => import('./smb-mindset-shift.jsx')) },
  { id: 'st001', label: 'ST-001: Feedback Loops', component: lazy(() => import('./feedback-loop-builder.jsx')) },
  { id: 'st002', label: 'ST-002: Leverage Points', component: lazy(() => import('./leverage-point-scorer.jsx')) },
  { id: 'st003', label: 'ST-003: Burden Shift', component: lazy(() => import('./burden-shift-simulator.jsx')) },
]
```

New artifact source files (copy from):
- `Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx` (ST-004)
- `Interactive Artifact for Cubelets/tool-orchestration-analyzer.jsx` (ST-005)
- `Interactive Artifact for Cubelets/automation-debt-simulator.jsx` (ST-006)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy artifact files and update App.jsx with week-grouped 9-tab layout</name>
  <files>
    preview-app/src/agent-feedback-loop-builder.jsx,
    preview-app/src/tool-orchestration-analyzer.jsx,
    preview-app/src/automation-debt-simulator.jsx,
    preview-app/src/App.jsx
  </files>
  <action>
1. Copy 3 new artifact JSX files from `Interactive Artifact for Cubelets/` to `preview-app/src/`:
   - `agent-feedback-loop-builder.jsx` (ST-004)
   - `tool-orchestration-analyzer.jsx` (ST-005)
   - `automation-debt-simulator.jsx` (ST-006)

2. Rewrite `preview-app/src/App.jsx` to:
   a. Add 3 new entries to the artifacts array:
      - `{ id: 'st004', label: 'ST-004: Agent Feedback', component: lazy(() => import('./agent-feedback-loop-builder.jsx')), week: 3 }`
      - `{ id: 'st005', label: 'ST-005: Tool Orchestration', component: lazy(() => import('./tool-orchestration-analyzer.jsx')), week: 3 }`
      - `{ id: 'st006', label: 'ST-006: Automation Debt', component: lazy(() => import('./automation-debt-simulator.jsx')), week: 3 }`
   b. Add `week` property to existing entries (week 1 for w1c1/w1c2/w1c3, week 2 for st001/st002/st003)
   c. Group tabs by week with visual separators in the nav. Use small week label headers (`Week 1`, `Week 2`, `Week 3`) styled in `#8b8fa3` at `10px` font-size above each group, separated by a thin `1px solid #2a2d3a` vertical divider between groups. Each week group is a flex column with the label on top and the tab buttons below in a row.
   d. Keep existing dark cybernetic styling: `#0f1117` background, `#161922` nav, `#00d4aa` active color, JetBrains Mono font
   e. Tab bar should handle 9 tabs gracefully -- use `overflowX: 'auto'` (already present), reduce padding to `10px 14px` to fit, font-size stays `13px`
   f. Keep `useState('w1c1')` as default active tab (first tab)
   g. No dagre dependency -- per user decision, built-in layout is sufficient (INTG-08 satisfied by absence)
  </action>
  <verify>
    <automated>cd preview-app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Preview app builds successfully with 9 tabs in 3 week groups, all lazy-loaded, no dagre dependency</done>
</task>

<task type="auto">
  <name>Task 2: Verify dev server renders all 9 tabs</name>
  <files>preview-app/src/App.jsx</files>
  <action>
1. Run `cd preview-app && npm run build` to confirm production build succeeds with all 9 artifacts bundled
2. Check build output shows 9 chunk files (one per lazy-loaded artifact)
3. If build fails, fix any import issues (missing dependencies, syntax errors in copied artifacts)
4. Verify no dagre import exists anywhere in the copied artifacts -- if found, the artifact uses built-in layout and dagre import should be removed or the import should be conditional
  </action>
  <verify>
    <automated>cd preview-app && npm run build 2>&1 | grep -E "chunk|error|warning" | head -20</automated>
  </verify>
  <done>Build produces 9 artifact chunks with zero errors, no dagre dependency in node_modules</done>
</task>

</tasks>

<verification>
- `npm run build` in preview-app succeeds
- Build output contains chunks for all 9 artifacts
- App.jsx has 9 entries in artifacts array with week grouping
- No dagre in package.json dependencies
</verification>

<success_criteria>
Preview app builds with 9 lazy-loaded tabs organized by week (3 groups of 3), using the established dark cybernetic styling, with no new dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/11-integration-deployment/11-01-SUMMARY.md`
</output>
