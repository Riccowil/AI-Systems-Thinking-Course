---
phase: 13-st-004-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx"
autonomous: false
requirements:
  - ST004-01

must_haves:
  truths:
    - "Student can paste valid JSON trace into import modal and see nodes appear on canvas with loops auto-detected"
    - "Student can upload a .json file and see the same result as pasting"
    - "Student can load a built-in sample trace (Retry Storm, RAG Pipeline, Multi-tool Agent) with one click"
    - "Invalid JSON shows a helpful error message with a valid snippet example"
    - "Student can undo import to restore previous canvas state"
    - "Import mode shows loop analysis immediately without requiring predictions"
  artifacts:
    - path: "Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx"
      provides: "Trace import modal, parseTrace function, sample traces, import mode state"
      contains: "SAMPLE_TRACES"
  key_links:
    - from: "parseTrace()"
      to: "setNodes/setEdges"
      via: "Import modal submit handler"
      pattern: "parseTrace.*setNodes"
    - from: "Import modal sample buttons"
      to: "parseTrace()"
      via: "onClick loads SAMPLE_TRACES constant into textarea then parses"
      pattern: "SAMPLE_TRACES"
    - from: "importMode state"
      to: "predictions gate bypass"
      via: "skipPredictions = exampleMode || importMode"
      pattern: "importMode"
---

<objective>
Add JSON trace import capability to ST-004 Agent Feedback Loops artifact: import modal with paste/upload, 3 built-in sample traces, parseTrace validation, auto-CLD mapping with loop detection, undo import, and import mode that bypasses the predictions gate.

Purpose: Enables the Import+Explore learning path where students analyze real agent execution traces instead of building CLDs from scratch.
Output: Modified agent-feedback-loop-builder.jsx with trace import feature complete.
</objective>

<execution_context>
@C:/Users/ricco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ricco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-st-004-enhancements/13-CONTEXT.md
@.planning/phases/13-st-004-enhancements/13-RESEARCH.md

<interfaces>
<!-- Key state and functions from agent-feedback-loop-builder.jsx that the executor must integrate with -->

Existing state variables (inside AgentFeedbackLoopBuilder component):
```jsx
const [nodes, setNodes] = useState([]);
const [edges, setEdges] = useState([]);
const [mode, setMode] = useState("select");
const [exampleMode, setExampleMode] = useState(false);
const [detectedLoops, setDetectedLoops] = useState([]);
const [predictions, setPredictions] = useState({});
const [allPredicted, setAllPredicted] = useState(false);
const [primerCollapsed, setPrimerCollapsed] = useState(true);
const [nodeCounter, setNodeCounter] = useState(0);
const [showTutorial, setShowTutorial] = useState(true);
```

Existing functions to reuse:
```jsx
function findCycles(nodes, edges) { /* DFS cycle detection, returns loop objects */ }
function classifyLoop(cycle, edges) { /* Returns "reinforcing" or "balancing" */ }
function scoreLoopSeverity(cycle, edges) { /* Returns 0-100 severity score */ }
function autoArrangeNodes(nodes, edges) { /* Topological sort layout */ }
const loadExample = () => { /* Sets nodes/edges from PRELOADED_EXAMPLE, sets exampleMode=true */ };
const clearAll = () => { /* Resets all state */ };
```

Node shape: `{ id, label, component_type, x, y }`
Edge shape: `{ from, to, polarity, strength, label }`

PRELOADED_EXAMPLE constant at top of file (lines 4-26): nodes array + edges array.
COLORS constant (lines 28-37): Dark cybernetic palette.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parseTrace function, SAMPLE_TRACES constant, and import state</name>
  <files>Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx</files>
  <action>
At the top of agent-feedback-loop-builder.jsx, after the PRELOADED_EXAMPLE constant:

1. Add SAMPLE_TRACES constant containing 3 traces — each is an object with `name` and `trace` fields:
   - "Retry Storm" — agent retries a failing tool 4+ times, accumulating tokens. Steps: call_api, parse_response, call_api (retry), parse_response, call_api (retry), error_handler. Creates a visible reinforcing loop.
   - "RAG Pipeline" — retrieve_documents, rerank_results, generate_answer, retrieve_documents (refinement loop), generate_answer. Shows retrieval-generation feedback loop.
   - "Multi-tool Agent" — search_web, extract_data, validate_data, search_web (re-search on validation failure), store_result. Shows validation-driven retry loop.

   Each trace follows the schema: `{ agent: string, steps: [{ tool_call: string, timestamp: number, tokens: number }] }`

2. Add parseTrace(jsonString) function after the existing utility functions (findCycles, classifyLoop, etc.):
   - Parse JSON with try/catch; on failure return `{ error: "Invalid JSON. Expected format:\n{\"agent\": \"...\", \"steps\": [{\"tool_call\": \"...\", \"timestamp\": 0, \"tokens\": 128}]}" }`
   - Validate required fields: `data.agent` (string), `data.steps` (non-empty array), each step has `tool_call` (string), `timestamp` (number), `tokens` (number)
   - On validation failure, return `{ error: "..." }` with specific message about which field is missing, plus a valid snippet example
   - Map steps to nodes: one "agent" node for `data.agent`, one "tool" node per unique `tool_call` value. Accumulate tokens and compute latency per tool (max timestamp for that tool - min timestamp). Node shape: `{ id, label, component_type, x, y, tokens, latency }`
   - Map steps to edges: agent -> tool for each unique tool_call. If the same tool appears in consecutive non-adjacent steps, also add tool -> agent edges to show the feedback pattern. Deduplicate edges using a Set on `${from}-${to}`.
   - Return `{ nodes, edges }` on success

3. Inside the component, add new state variables:
   - `showImportModal` (boolean, default false)
   - `importText` (string, default "")
   - `importError` (string, default "")
   - `importMode` (boolean, default false) — tracks whether current canvas is an imported trace
   - `previousCanvas` (object or null, default null) — `{ nodes, edges }` cached for undo

4. Add `handleImport` function:
   - Call `parseTrace(importText)` — if error, set `importError` and return
   - Cache current `{ nodes, edges }` into `previousCanvas` (for undo)
   - Run `autoArrangeNodes(result.nodes, result.edges)` on the parsed result
   - Set nodes, edges from arranged result
   - Run `findCycles` on new nodes/edges, set detectedLoops
   - Set `importMode = true`, `exampleMode = false`, `showImportModal = false`, `importError = ""`, `importText = ""`
   - Set `nodeCounter` to result.nodes.length
   - Set `showTutorial = false`

5. Add `handleUndoImport` function:
   - If `previousCanvas` is not null, restore nodes and edges from it
   - Set `importMode = false`, `previousCanvas = null`
   - Re-run `findCycles` on restored nodes/edges

6. Add `handleFileUpload` function:
   - Accept file input change event
   - Use `FileReader.readAsText()` to read the file
   - On load, set `importText` to the file content (which triggers validation on submit, not on load)

7. Update the predictions gate: wherever `allPredicted` is checked to reveal analysis, change the condition to `const skipPredictions = exampleMode || importMode;` and use `skipPredictions || allPredicted` to gate analysis reveal. The Import+Explore path shows loops tab analysis immediately.

8. In the header bar (above the canvas), add an "Import Trace" button styled consistently with existing header buttons. onClick sets `showImportModal = true`. Also add an "Undo Import" button that only renders when `importMode === true && previousCanvas !== null`, calling `handleUndoImport`.

9. Render the import modal when `showImportModal` is true — a full-screen overlay div at z-index 200 with:
   - Semi-transparent dark backdrop
   - Centered modal card (~500px wide) with COLORS.panel background
   - Title: "Import Agent Trace"
   - 3 sample trace buttons (one per SAMPLE_TRACES entry) that set `importText` to `JSON.stringify(trace, null, 2)`
   - Textarea (12 rows) pre-filled with `importText`, editable
   - File upload input: `<input type="file" accept=".json">` with onChange calling handleFileUpload
   - Error display: if `importError` is non-empty, show in COLORS.accentDanger with the error text (preserving newlines)
   - "Import" button (calls handleImport) and "Cancel" button (closes modal, clears error)
   - All inline styles using COLORS constants

10. Update `clearAll` to also reset `importMode = false` and `previousCanvas = null`.

Line budget: This task adds ~150-170 lines (SAMPLE_TRACES ~40, parseTrace ~40, modal JSX ~60, state+handlers ~30). Target total after this task: ~700-720 lines.
  </action>
  <verify>
    <automated>wc -l "Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx" | grep -E "^[6-8][0-9]{2}" && grep -q "SAMPLE_TRACES" "Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx" && grep -q "parseTrace" "Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx" && grep -q "importMode" "Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx" && grep -q "showImportModal" "Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx" && grep -q "previousCanvas" "Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - SAMPLE_TRACES constant with 3 traces (Retry Storm, RAG Pipeline, Multi-tool Agent)
    - parseTrace() validates JSON and maps to nodes/edges with deduplication
    - Import modal renders with paste textarea, file upload, sample buttons, error display
    - Import replaces canvas, sets importMode, caches previousCanvas
    - Undo Import restores cached canvas state
    - Import mode bypasses predictions gate (loops tab shows analysis immediately)
    - File stays under 750 lines
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify trace import in Claude.ai sandbox</name>
  <files>Interactive Artifact for Cubelets/agent-feedback-loop-builder.jsx</files>
  <action>Human verification: test trace import in Claude.ai sandbox. Copy artifact JSX, paste into Claude.ai, exercise import modal, sample traces, validation errors, undo import, and Import+Explore learning path.</action>
  <what-built>JSON trace import feature for ST-004 artifact: import modal with paste/upload/sample traces, CLD auto-mapping with loop detection, undo import, and Import+Explore learning path that bypasses predictions gate</what-built>
  <how-to-verify>
    1. Copy the full contents of agent-feedback-loop-builder.jsx
    2. Paste into Claude.ai as a new artifact
    3. Click "Import Trace" button in header — modal should appear
    4. Click "Retry Storm" sample button — textarea fills with JSON
    5. Click "Import" — modal closes, canvas shows nodes with detected loops in Loops tab
    6. Verify loops tab shows analysis immediately (no prediction gate)
    7. Click "Undo Import" — canvas restores to previous state (empty or worked example)
    8. Re-open Import modal, paste invalid JSON like `{"bad": true}` — verify helpful error message appears
    9. Try pasting completely invalid text like "hello" — verify JSON parse error message appears
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  <verify>
    <automated>echo "Manual checkpoint — human verifies in Claude.ai sandbox"</automated>
  </verify>
  <done>User confirms trace import works: modal opens, samples load, validation errors are helpful, undo restores state, import mode bypasses predictions</done>
</task>

</tasks>

<verification>
- parseTrace correctly validates the generic JSON schema and rejects malformed input with educational error messages
- Sample traces load and produce visible CLDs with at least one detected feedback loop each
- Import mode bypasses prediction gate while Build+Predict flow is unchanged
- Undo import restores exact previous canvas state
- File stays within line budget (~700-720 lines after this plan)
</verification>

<success_criteria>
Student can open the import modal, load a sample trace or paste custom JSON, see it auto-mapped as a CLD with feedback loops detected in the Loops tab, and undo the import to restore the previous canvas.
</success_criteria>

<output>
After completion, create `.planning/phases/13-st-004-enhancements/13-01-SUMMARY.md`
</output>
